# Pony 模板工作流：Codex执行规则

## 工具怎么知道"测的是哪个检出"（2026-09-24 修复）

在 worktree 里改模板、用 worktree 起预览时，必须让工具也指向同一个检出，否则证据会盖错提交号。
优先级：命令行 `--repo-root` > 环境变量 `PONY_REPO_ROOT` > `tasks/local.json` > `tasks/bootstrap.json`。

```powershell
$env:PONY_REPO_ROOT = 'C:/tyseo/cms-sport-tpl-bing-z1-verify'
.\.venv\Scripts\python.exe -B src\z_manual_preview.py            # 预览也从这个检出起
.\.venv\Scripts\python.exe -B src\red_line_gate.py --ids 2 --output runs/z2-gate-<时间>
```

红线门槛和链接审计每套都会调 `core.preview_source_check()`：把预览真吐出来的 css/js 与磁盘逐字节比对，
再要求磁盘 `master.html` 里的静态引用（含 `?v=`）原样出现在预览首页里。对不上就判 blocked 或直接中止，
不让盖错章的证据变成 pass。`gate.md` 抬头现在会打印检出路径、分支和提交。

渲染后的内容有独立的确定性检查 `src/z_content_rules.py`：页面上不该出现 None/undefined/NaN、
简介不该留 Markdown 原文（该用共享过滤器 `|rich_text` 而不是 `|safe`）、每页正好一个 h1。
这三条以前都靠人看页面才发现，现在改模板后随手跑一遍即可。

页面契约（`runs/z-v2-recheck-20260919/contracts`）是靠爬站内链接生成的，站内没有入口的页面
会永远记成"没有真实样例"而从不检查。人工按 `run.py` 路由核实过的补充地址放在
`config/extra-page-samples.json`，门槛和内容检查都会并进去测；往里加条目前必须自己访问过并拿到 200。

## 功能以 r62 为基线，直播跳转 nofollow（用户 2026-09-22 新增）

所有模板的通用功能范围先对照业务仓库当前 r62 的真实页面、链接与控件。r62 没有的搜索、主题切换、回顶等不再作为通用必备功能或公共测试失败；目标模板自行增加的功能仍作为该模板自有用例验证。**手机抽屉是明确保留的强制例外**：390 宽必须在当前页面打开可关闭、内部链接可点击的抽屉或浮层。现行布局、可达性、无 404/500、实际点击结果和证据要求不因缩小功能分母而放宽。每次测试记录 r62 基线提交，避免基线变化后沿用旧用例。

直播内页中“观看直播”等跳转至直播站的 `/play/<type>/<id>` 链接必须包含 `rel="nofollow"`；使用 `target="_blank"` 时同时保留 `noopener noreferrer`。该项只针对直播内页到直播站的跳转场景，加入源码与渲染 DOM 回归，缺失即失败。

## 交付红线：点得通、不报错、说得清（用户 2026-09-21 下午确认，优先于下文旧口径）

起因：Leo 2026-09-21 中午点名批评 z10/z11/z12 手机端"點一下菜單就會跳出新的頁面"、z13"這按鈕功能也是壞的""錄像頁面也直接報錯"，结论是"你就是改完根本沒檢查"；并说明"效果差的人會淘汰""你就說哪幾個模板你已經完全確認好了""主要頁面都還是要檢查"。用户据此定下三条红线，任何模板（含 z18 新模板）交付前必须满足：

1. **所有点击都要成功。** 每种页面类型上的每个链接、按钮、页签、筛选、下拉、搜索、分页、返回顶部、主题切换、菜单，PC（1280）和 H5（390）都要实际点过且结果正确。手机端导航必须是汉堡按钮 → 当前页上盖出来的抽屉或浮层，不得跳到新页面。只点 PC 不点 H5 视为没检查。
2. **不能有 404，本地接口和页面也不能 500。** 用户原话："本地啥样，发到线上就是啥样"。本地预览里的坏链接、报错页一律按线上事故处理，不得以"本地数据不全"放过（Leo 补充：本地资料与生产不同时以生产为准——这只用于解释数据差异，不是放过报错的理由）。
3. **404 / 500 先分清是谁的问题，再说清楚。** Rechard："咱们所有模版调用的后端数据，都是一样的，只是不同的模版，展示方式不同""没有对应的模版页，就要加一下"；Leo："你404也是要問Rechard，有時候是你參數根本不對，他那邊路由沒辦法匹配"。所以：`TemplateNotFound`、堆栈落在 `templates/zN` 里、链接地址或参数拼错（对照 `run.py` 的路由和其他模板族同一处写法）——都是前端的活，必须修；堆栈落在 `run.py`、`cache/` 等 Python 代码里才算后端问题。后端问题不在模板里绕过或造假，而是报给用户，由用户去找 Rechard 推进。

给用户的问题反馈必须具体到能照着复现：哪套模板、哪个页面（完整地址）、点了哪个按钮或链接（位置和文字）、目标路径、404 还是 500；500 要写报错类型、出错位置或接口地址、怎么触发；能查就写线上同一路径是否同样。**报告要配图**：截出问题页面并把要点的元素用醒目的框圈出来，再截一张报错页。只写"某模板有 404"不合格。执行细则、工具命令和报告格式见 `docs/CLAUDE_TEMPLATE_PASS_BRIEF.md`。正式条款写在 `docs/TEMPLATE_ACCEPTANCE_STANDARD_V2.md` 第 0 节（政策 2.5），工具门槛是 `src/red_line_gate.py`（`--ids` 指定模板或 `--changed-since` 查改动过的模板）：门槛不通过的模板不能说"已确认"、不能提交；已查明的后端问题登记在 `config/backend-issues.json`。

已查明的共性后端问题（截至 2026-09-21）：所有模板的 `/tags/<id>`、`/tag/<id>.html` 本地 500——`run.py` 的 `tag_list` 调 `recovery_id_offset(id)`（`cache/cache_data.py`），站点 `OffsetNum` 为空/0 时原样返回字符串 id，随后 `recovery_id <= 0` 抛 `TypeError`；模板链接写法与其他模板族一致，模板侧改不了。

## 上站选版与 z18 开发是两条独立事项（用户 2026-09-21 最新说明）

用户转发 2026-09-21 11:02–11:06 Leo 回复链，纠正此前“上站默认 r62”：每次上站从最新 `bing 指数词汇总` 的 **Pony 可用模板编号范围**中选一个，范围随团队维护更新；不得固定 r62，也不得因 Pony 自己开发 z18 就自动使用 z18。2026-09-21 在线 `bing体育品牌词` 页 Pony 行 M17 实查为 `z1~z17`，L17 的 `z、d、t、k` 是另一分组，不能混用。选版须在最终写 E:H 前重新读该行、核对选中编号仍可用并审计该模板实际能力，按能力复核 TDK；表格更新后随之更新可选范围。Pony 继续只在 `templates/z18/`、`static/z18/` 开发四个周设计版本，开发编号不等于当前上站可选编号。当前本地工具的 `tdk-policy.json`/`generate_tdk` 仍有 r62 固定门禁，未改程序前不能声称它已实现动态选版；人工/AI按本条核实后写表，不依赖旧默认值。

## 每周模板交付与 z18 分配（用户 2026-09-21 最新确认）

用户明确将 `z18` 分配为 Pony 专属模板编号。本轮 z18 新模板制作授权覆盖已确认编号的 `templates/z18/`、`static/z18/`，替代下文“z18 由他人使用”的旧临时修复边界；不推导 z19–z21 或其他编号的修改权限。业务仓库已有 z18 源码和他人旧分支，开工须审计并保留现状，另用本轮个人分支开发，不接管旧分支。

每周目标为在 Pony 专属 `z18` 编号下交付四个整站设计版本，逐版基于已扫描的模板参考库，结合本轮新设计的样式和布局，尽可能拉开结构与视觉差异。每版都执行现行逐页验收，通过后按业务仓库版控流程提交并推送本版授权文件；不能把四套目标理解为四种配色或四个域名。不处理其他模板编号。一个 z18 目录在任一 Git 提交只对应一个生效版本，四次交付分别保存设计、证据和提交，历史版本通过提交记录追溯；不假称四版同时可供上站选择。此周交付要求不自动创建调度，也不放宽公司人审、合并、部署边界。

## 上站表判据与未来机器人交接（用户 2026-09-21 新增）

当批域名写入“域名待购买”并回读后，就可依据有效分配词、当次竞品和模板能力准备新 TDK 草稿。采购完成的工作流判据是对应日期的域名出现在 Bing“上站202609”表中，且归属为 Pony、域名和分配词逐项匹配；待购买表中删除或清空不是采购成功证据，域名仍在待购买表也不单独阻断已确认的上站表行。只有上述上站表行出现并确认 E:H 可写、内容无冲突后，才自动填写该行 Template/Title/Description/Keyword；写后回读 E:H 和未授权列。机器人接入后须在工作平台通知用户“TDK 已填可手动复制”，通知以写后回读成功为前提；无机器人时由当前对话报告，不声称已发机器人通知。

未来用户明确启动机器人自动上站时，先读取**每个目标行** M 列 `ServerName`，与准备打开的 `https://s213017.abcd-cms.com/mgradm/optdata.html` 的服务名核对。`s213017` 只是用户本次提供的后台地址，不得固定使用；若行值不同、缺失、混用多个服务或不能确认映射，停止该行后台操作并报告。截图为 Curry 的示例行，不能代替 Pony 行的 M 列值。当前仅记录标准，不接入/启动机器人、不打开后台、不复制脚本或提交网站。

## 每日域名与 TDK（用户 2026-09-19 补充）

用户 2026-09-21 补充（域名命名风格）：站点主要面向中国大陆用户，生成上站域名时**优先使用拼音或拼音首字母缩写**，并参考 Leo 和其他同事已经在用的域名风格——用户观察到其他人都是这么做的。每次生成前先看上站表、待购买表和 TG 聊天里别人近期实际用的域名，归纳构词方式后按同样风格去起，不要起成纯英文单词堆砌或与团队风格明显不同的名字。2026-09-21 在 TG 聊天里实查到的同事用例（只作风格参考，不得复用）：Leo 的 `live-daishutiyu.cn`、`88-live-mgtv.com`、`new-nbatv-qq.com`、`live-sqd-qq.com`，Rechard 的 `cn-360zhibo8-mgtv.com`、`pv-lvyinzhibo.com`——常见写法是"短前缀（live / new / cn / 数字）+ 品牌或赛事词的拼音或首字母缩写（daishutiyu、zhibo8、lvyinzhibo、sqd）+ 视情况带一个大陆用户熟悉的平台词"，用连字符连接。后缀、查重、注册商核验等其余要求不变。

用户 2026-09-21 补充：若当天未特别指定数量，每批按先前惯例选择 **20 个新域名**；当次明确给出其他数量时以当次为准。20 是每日默认批量，不是每套模板固定可上 20 站。

域名可用性核验是必要流程：逐域名查询注册状态，并在 Gname 等注册商核实可购买状态和价格，保存查询网站、时间及结果。RDAP 无记录只算注册局初查，不能替代注册商确认。验证码、登录或其他限制未解决时标记“注册商待核验”，不能宣称全部可买或核验完成。2026-09-19 已登记的 20 个域名仍有此待核验项；记录规则不等于补验通过，也不授权采购。

用户明确：说“上站”时若未提供新分配词，默认沿用前一天的有效分配词；提供了新词就用最新一批。2026-09-19 沿用并由用户重新列明的 27 个词见 `docs/SITE_LAUNCH_RUNBOOK.md`。域名后缀仅限 `.com`、`.cn`、`.com.cn`。不因在线轮换表未列 Pony 而自行换词或阻断已明确分配词。每日域名、TDK 均须重新准备且不得重复。域名写入前同时对当前批次、待购买表、上站历史及本地历史记录查重，并重新查询注册状态。每批 Title、Description、Keywords 按实际站点能力与当次竞品研究重新撰写，逐字段对本批及历史查重，不能只换标点、空格或域名。关键词池继续有效不等于允许复用旧文案。数量未建立固定日配额，不能由关键词数或模板数推断。现有采购、提交、上线边界不变。

## 修复范围更正：z1–z21（用户 2026-09-24）

用户明确指出 **z19–z21 也是本人负责修复的旧模板**，修复范围是 **z1–z21 共 21 套**，
不是下文 09-19 那版的 z1–z17。下面 09-19 那节"z18–z21 由他人使用，不得修改"已被此条取代，
保留作历史。z18 已由 Leo 合并进 main；z22 是本周新模板编号（Leo 09-23 确认"用z22"），
这两个属于"做新模板"，与"修复旧模板"分开算。

一套一个 PR 交付（用户 2026-09-24）：不再把多套打包成一条分支。每套从最新 main 切干净分支，
只含该编号的 `templates/zN` + `static/zN`，起预览跑红线门槛，通过后单独开 PR。

## 当前 z 系列修复边界（用户 2026-09-19 最终确认，已被上一节取代）

仅允许保留或修改 `templates/z1`–`templates/z17`、`static/z1`–`static/z17` 的任务改动，只修各套已有页面。z18–z21及其余模板由他人使用，不得修改。此前z18–z21任务改动已通过 `c223e6925` 撤回；旧z1–z21任务/报告仅是历史，不构成继续修改授权。

以上编号仅属于本次任务；下周或新任务依用户明确分配新建独立TaskSpec，不能自动沿用或扩张当前授权。通用验收标准不绑定z编号。

## 验收 v2：工具优先（用户 2026-09-19 补充）

政策2.2硬门槛（Leo发现z16缺陷后用户明确）：逐个内页筛选必须验证可见结果的ID集合与实际条件一致，含组合、恢复全部和空结果；按钮高亮不算通过。布局完整性按具体组件及断点的设计验证，禁止挤占后意外换行、错位、重叠、裁切、遮挡；不强制所有Logo/队名/比分一线对齐。`filter_results` 和 `layout_integrity` 为必需证据项，缺证据不得放行；不存在该模块才可有依据地N/A。首页交互通过不能代替球队资料、赛程、详情关联列表等内页检查。

2026-09-19最新补充：验收以日常样式版面＋轻微压力测试为准，不要求刻意放大字体/浏览器缩放、强制扩大文字间距后的效果。政策2.1关闭这些人工压力用例，旧结果仅作历史参考、不作为修复或放行阻断。轻压指小屏/断点附近/横屏、实际长队名标题、三位数比分、缺图、菜单展开等真实使用边界；全元素防挤占重叠、双主题、SEO、功能和证据要求仍有效。新标准适用于后续明确分配的模板，不绑定z系列。旧批次不能过滤后直接冒充新政策全量通过。

现行标准为 `docs/TEMPLATE_ACCEPTANCE_STANDARD_V2.md`，涵盖全部实际标签元素的挤压、裁切、碰撞、遮挡、点击区、媒体和布局稳定性，不限于文案。所有页面先由工具执行并保存证据；确定性失败先修复复测。仅工具无法覆盖/判定的项目进入AI队列，不重复工具已完整解决的检查。未执行留在工具工作队列，能力缺口不得默认通过。本文及旧文档中“逐页AI”按此新方式执行；逐页覆盖和公司人审仍保留。

## 每周模板参考库（2026-09-18 晚间用户补充）

用户每周会发起四套整站模板制作；开工先读 `catalog/README.md`、`docs/PONY_TEMPLATE_LIBRARY.md` 并刷新全量源码索引。Sit明确同模板可多域名复用，不限定每套20站；风格和排版尽量做出差异，参考x66–x80。索引/旧组件是候选，未经契约和全页验收不能计为通过。四套编号按团队分配确认；按对话发起，不自动调度或上站。此前“每两天一套”仅作历史背景。

## 每套模板固定验收门槛（用户2026-09-18明确确认）

后续每套整站模板按 `docs/TEMPLATE_ACCEPTANCE_STANDARD.md` 独立验收：逐页完成响应式与双主题、实际交互、SEO、代码/变更边界检查，并保存当前版本证据。缺页、未执行或blocked不能计为通过；无未解决P0/P1且完整检查完成后才标 `ready_for_human_review`。主管验收、上线、Bing抓取分别记录；不以工作流命令成功代表所有项目通过。

本文件只属于**外部工作流目录**，不是授权覆盖公司业务仓库根目录AGENTS.md。开始时同时读取业务仓库已有有效指令和本包PRD。

## 任务目标

为已有 `cms-sport-tpl-bing` Python/Jinja项目实施本地可重复工作流，生成一套完整网站模板并自动验收。不是另起一个静态站，也不是生成一批独立新闻正文。

## 永久工程边界

1. 业务仓库本任务只允许写已确认目标编号的 `templates/{id}/`、`static/{id}/`。不得使用过宽的 `templates/**` 白名单。
2. 不改任何Python、requirements、.env、路由、数据库、共享模板、其他模板、仓库根配置或GitHub Actions。r62只作参考，除非后续任务明确要求修改r62。
3. 工具、测试、依赖、夹具和报告均在外部工作区。不要为了方便把这些内容塞进公司仓库或修改.gitignore。
4. 本地环境已经跑通。先读取现有代码/配置结构并提取契约，不重新搭建或自动更改白名单。
5. 可以只读分析后端，但不得发明接口、变量、URL、比分、搜索能力或改后端补数据。需要后端时输出给Rechard的具体依赖。
6. 保留现有域名工作流。它仅到选域名和自动写待购买表；不要重新写首批20行，不自动做TDK、采购、发布、IndexNow或定时任务。
7. 现有table-write授权仍有效；本次模板任务不撤回它，也不触发它。
8. 新模板编号必须符合团队分配且无冲突；未知时做外部草稿与工具，不擅自选择r63。不得新建假编号混入业务仓库。
9. 不自动push、开PR、合并、部署、发群；本地完成后给报告。后续明确的外部操作要求另行执行。
10. 保存用户已有工作树；不reset --hard、不git clean、不覆盖他人文件。实际写入前检查路径，结束检查文件增量。

## 制作规范

- 先审计r62全部页面、继承/宏、变量和路由，之后生成设计与代码；以真实本地证据为准。
- 每套内部一致、套间主要布局不同；不是换色、换类名或随机DOM。功能与SEO语义可以复用。
- 保留真实Jinja数据绑定，不输出某次响应的硬编码复制品。
- 必需功能：RWD、dark/light、真实搜索、goto top、手机合适的drawer/popover导航、逐页视觉与AI SEO检查。
- 搜索要有真实行为，不把按钮或本页过滤冒充站内搜索。
- 比分区分0、null、undefined、空字符串；长队名、三位数比分、未开始/取消/完场等状态必须测试。
- 不把 `default(..., true)` 或JS的 `||` 用作不加区分的比分兜底。
- 新模板不依赖r62私有CSS/JS/父模板；已存在的共享依赖只能按契约只读使用。
- 正式数据与夹具分离；不要新增随意的 `|safe`，不将秘密放进日志/报告。

## 检查与结果

全部实际页面类型都要跑原始HTML、链接/SEO、交互、响应式/主题矩阵；无JS检查不能省略。工具逐页留证并判定，AI处理明确的能力缺口/歧义，不能只凭首页下结论。

允许在边界内连续自动修复，不逐页询问是否继续。纯样式选择自行形成设计规格；真正缺接口/编号时只阻断相关部分并继续独立任务。

默认每阶段三轮修复，超过后准确报告。不得删除测试、降低标准、隐藏比分、随意更新截图基线来获得PASS。

每个测试结果分为pass/fail/blocked/needs_review/not_applicable；未执行不得pass，N/A需要依据。记录real_app与fixture环境，不能相互替代。

工具完成、真实联调通过、AI检查通过、人工验收、已部署、已被Bing抓取是不同状态。只声称实际有证据的状态。

## 固定工作流与临时任务

z1–z21 检查修复是临时安排，不是固定流水线。具体来源、历史结果和手动入口见 docs/TEMPORARY_TASKS.md、tasks/ad-hoc.json。它仍保留待处理状态，但不得在每日上站后自动触发。

固定流程仅写“继续当时安排的模板开发或维护任务”，目标编号与范围由当次任务确定。配置注册表不携带某个临时任务作为默认下一步。采购等待扫描和完整自动上站尚未启用。

## 2026-09-18 半自动验收增量与迁移授权

当前任务为完善外部半自动工作流；读取 docs/ACCEPTANCE_INCREMENT.md。沿用公司强约束，不将其误作业务模板批量修改许可。验收完整计划、HTTP/DOM/无JS证据、输入失效、逐页AI复核及人审分开；旧首轮截图不能升级为全量通过。

CLI按用户指定使用0.1.20；升级由另一任务负责，本任务只核验和适配。不混淆CLI与Python Playwright版本。RS助手机器人仅在config/integrations.json预留且disabled，不读取其秘密、不启动任务/定时器。

用户明确授权将公司业务仓库之外的工具、数据、文档和工作流推送 maolaila/tyseo，包括需要跨机迁移的tySEO凭据。该仓库已核验为公开，因此凭据和运行数据只以AES-GCM密文分片上传，密钥在仓库外。此授权只覆盖工具库推送，不覆盖公司仓库push/PR/merge、表格或发布。迁移步骤见docs/MIGRATION.md。


## 当前触发方式（用户最新确认）

当前由用户对话发起任务，AI调用既有工具推进已授权步骤；无需用户手动逐条执行命令，也不逐步重复请求已授予的许可。机器人和定时调度保持关闭。用户待RS机器人优化完成后会明确通知接入，再实现并验证程序启动后的自动推进；现在不提前接入或开启。自动执行仍遵守公司规范、任务范围和人工review要求。

## 当前工作台范围（用户最新确认）

工作台只展示本批 TDK，并在用户点击后将已核对文案写入上站表本人行 E:H、回读确认。没有30分钟或其他后台监听，也没有域名采购、脚本复制、后台登录/回填、提交、上线核验入口。后续由用户手动处理；z1–z21仍是独立临时任务。以前有关工作台监听和自动上站的段落是历史记录，当前以本节和 SITE_LAUNCH_RUNBOOK.md 为准。


## 业务浏览器登录方式

用户明确要求：以后表格、后台等这类业务连接都不用无痕模式。使用普通浏览器及持久化配置，保留登录状态；不使用访客或临时配置来代替。当前程序profile为private/launch-browser，使用--headed --persistent --profile启动。首次需要登录不代表无痕；不得擅自清除该配置或导出其他浏览器的登录凭据。


## TDK 来源与查重

用户 2026-09-21 补充：写当批 TDK 前，逐一查看 `bing 指数词汇总` 中与本批体育词相关的页签；有竞品标题/描述的页签用于参考关键词组合和表达节奏，只有关键词、分组或排名的页签只用于选词/背景，不能当成现成 TDK。再只读查看 Bing 上站表中 Leo 过往日期、相近体育词的 Title/Description/Keyword，参考团队常用的标题层次、关键词密度和行文长度，使风格协调。记录看过的页签、Leo 样本日期/行号；参考不等于复制，不能沿用相同句子、替换品牌名冒充新稿，或复写未核实的“官方、高清、无延迟、免费”等承诺。当前 Pony 词分配、目标模板真实能力、当次竞品核查及本批/历史逐字段查重仍是写入门槛；Leo 的历史 TDK 是风格参考，不自动产生每批审核要求。

每次上站重新读取 bing 指数词汇总 中最新的 bing体育品牌词 及 Pony 当期归属，不能固定分组或默认 all。左侧关键词分组与右侧模板分组是不同信息；日期过期的分配不能继续使用。找不到当次 Pony 归属时暂停新稿，记录待分配，不自选旧组。

AI 结合本次分配词、当次核实的竞争网站页面和目标模板真实能力撰写 Title、Description、Keywords。竞争网站只作参考，不照抄，也不假称官网或未提供的功能。每次上站写新的文案；新批次先对本批及历史记录逐字段查重，不能只改标点冒充新稿。工作台不再运行按关键词哈希选择三种固定句式的旧生成器。

Leo 审核并非日常固定关卡；只有当次明确要求时才作为写表前提。2026-09-18本批用户已转述审核通过，20份 E:H 已在本人行3539–3558回读确认，保持原样。写表目标只有工作簿 1y7VWR6T4ABcd09qa51GM5-7XXGC80w1h21wCL4stvUg 的 Bing 页 gid=2066853497；参考关键词表只读。工作台只处理这一写表步骤，其余由用户手动处理。详见 SITE_LAUNCH_RUNBOOK.md。
