# Pony 模板工作流：Codex执行规则

本文件只属于**外部工作流目录**，不是授权覆盖公司业务仓库根目录AGENTS.md。开始时同时读取业务仓库已有有效指令和本包PRD。

## 任务目标

为已有 `cms-sport-tpl-bing` Python/Jinja项目实施本地可重复工作流，生成一套完整网站模板并自动验收。不是另起一个静态站，也不是生成一批独立新闻正文。

## 永久工程边界

1. 业务仓库本任务只允许写已确认目标编号的 `templates/{id}/`、`static/{id}/`。不得使用过宽的 `templates/**` 白名单。
2. 不改任何Python、requirements、.env、路由、数据库、共享模板、其他模板、仓库根配置或GitHub Actions。r62只作参考，除非后续任务明确要求修改r62。
3. 工具、测试、依赖、夹具和报告均在外部工作区。不要为了方便把这些内容塞进公司仓库或修改.gitignore。
4. 本地环境已经跑通。先读取现有代码/配置结构并提取契约，不重新搭建或自动更改白名单。
5. 可以只读分析后端，但不得发明接口、变量、URL、比分、搜索能力或改后端补数据。需要后端时输出给Rechard的具体依赖。
6. 保留现有域名工作流。它仅到选域名和自动写待购买表；不要重新写首批20行，不自动做TDK、采购、发布、IndexNow或定时任务。
7. 现有table-write授权仍有效；本次模板任务不撤回它，也不触发它。
8. 新模板编号必须符合团队分配且无冲突；未知时做外部草稿与工具，不擅自选择r63。不得新建假编号混入业务仓库。
9. 不自动push、开PR、合并、部署、发群；本地完成后给报告。后续明确的外部操作要求另行执行。
10. 保存用户已有工作树；不reset --hard、不git clean、不覆盖他人文件。实际写入前检查路径，结束检查文件增量。

## 制作规范

- 先审计r62全部页面、继承/宏、变量和路由，之后生成设计与代码；以真实本地证据为准。
- 每套内部一致、套间主要布局不同；不是换色、换类名或随机DOM。功能与SEO语义可以复用。
- 保留真实Jinja数据绑定，不输出某次响应的硬编码复制品。
- 必需功能：RWD、dark/light、真实搜索、goto top、手机合适的drawer/popover导航、逐页视觉与AI SEO检查。
- 搜索要有真实行为，不把按钮或本页过滤冒充站内搜索。
- 比分区分0、null、undefined、空字符串；长队名、三位数比分、未开始/取消/完场等状态必须测试。
- 不把 `default(..., true)` 或JS的 `||` 用作不加区分的比分兜底。
- 新模板不依赖r62私有CSS/JS/父模板；已存在的共享依赖只能按契约只读使用。
- 正式数据与夹具分离；不要新增随意的 `|safe`，不将秘密放进日志/报告。

## 检查与结果

全部实际页面类型都要跑原始HTML、链接/SEO、交互、响应式/主题矩阵；无JS检查不能省略。AI逐页看图并给出证据，不能只凭首页下结论。

允许在边界内连续自动修复，不逐页询问是否继续。纯样式选择自行形成设计规格；真正缺接口/编号时只阻断相关部分并继续独立任务。

默认每阶段三轮修复，超过后准确报告。不得删除测试、降低标准、隐藏比分、随意更新截图基线来获得PASS。

每个测试结果分为pass/fail/blocked/needs_review/not_applicable；未执行不得pass，N/A需要依据。记录real_app与fixture环境，不能相互替代。

工具完成、真实联调通过、AI检查通过、人工验收、已部署、已被Bing抓取是不同状态。只声称实际有证据的状态。

## 固定工作流与临时任务

z1–z21 检查修复是临时安排，不是固定流水线。具体来源、历史结果和手动入口见 docs/TEMPORARY_TASKS.md、tasks/ad-hoc.json。它仍保留待处理状态，但不得在每日上站后自动触发。

固定流程仅写“继续当时安排的模板开发或维护任务”，目标编号与范围由当次任务确定。配置注册表不携带某个临时任务作为默认下一步。采购等待扫描和完整自动上站尚未启用。

## 2026-09-18 半自动验收增量与迁移授权

当前任务为完善外部半自动工作流；读取 docs/ACCEPTANCE_INCREMENT.md。沿用公司强约束，不将其误作业务模板批量修改许可。验收完整计划、HTTP/DOM/无JS证据、输入失效、逐页AI复核及人审分开；旧首轮截图不能升级为全量通过。

CLI按用户指定使用0.1.20；升级由另一任务负责，本任务只核验和适配。不混淆CLI与Python Playwright版本。RS助手机器人仅在config/integrations.json预留且disabled，不读取其秘密、不启动任务/定时器。

用户明确授权将公司业务仓库之外的工具、数据、文档和工作流推送 maolaila/tyseo，包括需要跨机迁移的tySEO凭据。该仓库已核验为公开，因此凭据和运行数据只以AES-GCM密文分片上传，密钥在仓库外。此授权只覆盖工具库推送，不覆盖公司仓库push/PR/merge、表格或发布。迁移步骤见docs/MIGRATION.md。


## 当前触发方式（用户最新确认）

当前由用户对话发起任务，AI调用既有工具推进已授权步骤；无需用户手动逐条执行命令，也不逐步重复请求已授予的许可。机器人和定时调度保持关闭。用户待RS机器人优化完成后会明确通知接入，再实现并验证程序启动后的自动推进；现在不提前接入或开启。自动执行仍遵守公司规范、任务范围和人工review要求。

## 当前工作台范围（用户最新确认）

工作台只展示本批 TDK，并在用户点击后将已核对文案写入上站表本人行 E:H、回读确认。没有30分钟或其他后台监听，也没有域名采购、脚本复制、后台登录/回填、提交、上线核验入口。后续由用户手动处理；z1–z21仍是独立临时任务。以前有关工作台监听和自动上站的段落是历史记录，当前以本节和 SITE_LAUNCH_RUNBOOK.md 为准。


## 业务浏览器登录方式

用户明确要求：以后表格、后台等这类业务连接都不用无痕模式。使用普通浏览器及持久化配置，保留登录状态；不使用访客或临时配置来代替。当前程序profile为private/launch-browser，使用--headed --persistent --profile启动。首次需要登录不代表无痕；不得擅自清除该配置或导出其他浏览器的登录凭据。


## TDK 来源与查重

每次上站重新读取 bing 指数词汇总 中最新的 bing体育品牌词 及 Pony 当期归属，不能固定分组或默认 all。左侧关键词分组与右侧模板分组是不同信息；日期过期的分配不能继续使用。找不到当次 Pony 归属时暂停新稿，记录待分配，不自选旧组。

AI 结合本次分配词、当次核实的竞争网站页面和目标模板真实能力撰写 Title、Description、Keywords。竞争网站只作参考，不照抄，也不假称官网或未提供的功能。每次上站写新的文案；新批次先对本批及历史记录逐字段查重，不能只改标点冒充新稿。工作台不再运行按关键词哈希选择三种固定句式的旧生成器。

Leo 审核并非日常固定关卡；只有当次明确要求时才作为写表前提。2026-09-18本批用户已转述审核通过，20份 E:H 已在本人行3539–3558回读确认，保持原样。写表目标只有工作簿 1y7VWR6T4ABcd09qa51GM5-7XXGC80w1h21wCL4stvUg 的 Bing 页 gid=2066853497；参考关键词表只读。工作台只处理这一写表步骤，其余由用户手动处理。详见 SITE_LAUNCH_RUNBOOK.md。
