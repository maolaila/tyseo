# z 系列模板修复进度（接着做之前先读这里）

安排（用户 2026-09-21 定）：除了每周四套新模板和每天上站之外，每天额外做两三个 z 系列模板的修复，**做哪几个由用户当天指定**，不要自己扩大范围；主管在别的模板上指出的问题先记在这里，排到那个模板时再修。每套交付前必须过政策 2.5 的红线门槛（`docs/TEMPLATE_ACCEPTANCE_STANDARD_V2.md` 第 0 节、`src/red_line_gate.py`），执行细则见 `docs/CLAUDE_TEMPLATE_PASS_BRIEF.md`。

业务仓库 `C:\tyseo\cms-sport-tpl-bing`，分支 `fix/pony-z1-z17-layout`（PR 草稿 #1664）。每套修完单独提交 `fix(zN): …` 并推送到这个分支。

## 各套状态

| 模板 | 状态 | 说明 |
|---|---|---|
| z1、z2、z3 | 2026-09-21 在修（当天指定） | 结果和证据见各自的报告：`runs/z1-claude-20260921` 等 |
| z18 | 2026-09-21 新做整套（当天指定），用户次日提交 | 预览 6318；只改 `templates/z18`、`static/z18`；新个人分支，不接管 `origin/feat/dev-z18` |
| z4 | 做了一半，已停 | 见下文"已停的半成品" |
| z10、z11、z12 | 做了一半，已停；**Leo 点名的手机菜单已改成抽屉**，没有全量验证 | 见下文 |
| z13 | 做了一半，已停；**Leo 点名的两个问题都已改好并在浏览器里验证过** | 见下文 |
| z5–z9、z14–z17 | 本轮还没开始 | 2026-09-21 14:12 全量链接检查（`runs/link-audit-all-20260921-claude`）里记下的线索见下文，**还没逐条查原因** |

## 已停的半成品（2026-09-21 约 16:35 停手）

每套的改动都存成了补丁，业务仓库里这几个目录已恢复到提交 `f0c3489d4` 的样子（补丁的基线也是它）。接着做某一套时：

```
cd C:\tyseo\cms-sport-tpl-bing
git apply --check C:\tyseo\pony-template-workflow\runs\partial-work-20260921\zN\zN.patch
git apply C:\tyseo\pony-template-workflow\runs\partial-work-20260921\zN\zN.patch
```

然后读同目录的 `STATUS.md`（改了什么、验证到哪、发现的问题、后端问题清单、下次从哪开始），第一步都是先确认本地预览正常，再把"改了但没在浏览器里验证"的项逐个验证。补丁要整份应用，不要只挑几个文件（z10 的 5 个菜单文件互相依赖）。

- **z4**（补丁 16 个文件）：手机菜单锁滚动和关闭按钮、比赛时间被截断、内页"直播"按钮在 1280 被裁（66 页从 112 处降到 0）、h1 和一处暗色对比度——这些验证过；第二批对比度、`<sapn>` 拼写、联赛链接改小写、全部赛程页嵌套 `<main>` 没验证。z4 缺 18 个页面模板（没有页面链到它们），补不补要用户定。后端 9 条（含标签页 500、NBA 历届冠军里历史球队 500、录像/集锦分页偶发 500、东亚女足对阵历史 404），配图没截。
- **z10**（补丁 6 个文件）：Leo 说"點一下菜單就會跳出新的頁面"——原因是旧菜单是一整屏不透明面板、没有遮罩，看起来像跳了新页面；改成左侧滑出 300px 抽屉（遮罩、Esc、锁滚动，980 以下出汉堡），手机亮色 14 个页面实测通过、抽屉里 16 个链接全部正确；暗色只测 1 页，联赛子页/球队/球员页没点。`/cba` 500、`/luxiang/zuqiu-N.html` 分页 500 查明是后端（Redis 超时；缓存读到空内容 `JSONDecodeError`，`cache/cache_data.py` 4792 行）。没修：暗色顶栏"搜索"字看不清（所有页）、7 个页面两个 h1、`/nba/saicheng` 30 张队徽请求 `/nba/None` 404、历届冠军队徽变形、缺十几个页面模板。后端 8 条，配图没截。
- **z11**（补丁 13 个文件）：Leo 说"z11也是"——同样是整屏面板；改成顶栏按钮 + 左侧抽屉，390 宽 32 个页面里 31 个通过（空搜索地址被后端跳回首页），48 次链接点击全对，暗/亮/768 都看过；1280/1440 没确认汉堡隐藏。另修好首页比分被队徽盖住、标题扫光遮字、安卓下载条被底栏盖住等；有 3 处没验证。没修：专题列表挤成一段、搜索结果没样式、联赛介绍 ▼ 按钮无效、Markdown 原文直接显示。
- **z12**（补丁 21 个文件）：Leo 说"我看z12也是"——同样原因，还有桌面左下角露出没样式的按钮；改成抽屉，手机亮色 46 个样例页全部通过；暗色只测首页。另修 12 处：足球/篮球频道"全部球队""录像"坏链、`/CBA` 大写链接（模板用了数据里的大写，已改）、h1、非法标签、手机端表格和队名被截等；4 处没验证。后端：`/zuqiu/teams`、`/lanqiu/teams`、`/zuqiu/luxiang`、`/lanqiu/luxiang` 返回 500 而不是 404，配图没截。
- **z13**（补丁 11 个文件）：
  - Leo 说"z13這按鈕功能也是壞的"（今日赛程 全部/直播中/未开始/已结束）——**前端问题**：脚本隐藏了不匹配的行，但 z13 自己的 CSS 把隐藏盖掉了；页签数字也数的是全部比赛。已修，14 个页面 × 手机/电脑 × 明/暗 56/56 通过（当时本地没有进行中的比赛，"直播中""已结束"是临时改标签测的）。标题在跨天时改成"近期赛程"、每行带日期——**这个显示改动要用户确认**。
  - Leo 说"錄像頁面也直接報錯"并问"你跟我說你改在哪？"——**前端问题：z13 把地址拼错了**。足球频道的"录像"链到 `/zuqiu/luxiang`，这个地址后端只支持联赛（`run.py` 5230 行 `list_luxing` 抛 `IndexError`），频道级录像页应该是 `/luxiang/zuqiu-1.html`。已改，足球/篮球频道 5 个按钮 × 两种宽度 20/20 返回 200。**z12、z14 也链到了同样的错地址**（z12 已在补丁里改，z14 还没改）。配图在 `runs/z13-claude-20260921/backend-evidence/`。
  - 频道页积分榜"完整 >"指向不存在的页面，已按条件去掉——**要用户确认**。另：手机菜单加了关闭按钮和锁滚动；h1、视频图标、无 JS 的五大联赛三处没验证。没修：首页 1280 快速入口、队名、热门赛事图片被裁。

## 还没开始的模板：2026-09-21 全量链接检查里的线索（未查原因，排到时先按"二之二"分清是谁的问题）

- 所有模板：`/tags/<id>` 500——**已查明是后端**（登记在 `config/backend-issues.json`，已告诉用户，待发给 Rechard）。
- z14：足球频道"录像"链到 `/zuqiu/luxiang`（和 z13 同一个前端错误）。
- z7、z15、z16、z17：`/lanqiu/tulanjia`（土篮甲）500；z10、z12、z16、z17：`/cba` 500（z10 查明是 Redis 超时时的后端错误，恢复后要复查）。
- z15、z16、z17：大量联赛频道、球员、球队、新闻详情链接当时超时没拿到结果（`/lanqiu/wnba`、`/zuqiu/puu23`、`/yingchao/teams/teaminfo-N.html` 等），恢复后重查。
- z17：首页 `/ouguanbei/teams/teaminfo-N.html` 404（"那不勒斯 欧冠杯"）；z6、z15：`/zuqiu/news/N` 分页 500；z9：`/zuqiu/shipin-1` 500；z7：`/yayunnvzu/N.html` 500；z1：`/yingzuzongbei/N.html`、`/luxiang/fajia/N.html` 500。

## 环境

本地预览依赖远端测试 Redis / Mongo（`154.82.114.12`），按公网 IP 白名单放行。用户换网络（例如手机热点）后公网 IP 会变，所有预览首页都会 500，报 `redis.exceptions.TimeoutError`——这是环境问题，不是模板问题，也不能算验过。处理：用户在 Jenkins `dev_white_ip_manage`（Luis Walter 2026-09-18 给的入口）填新 IP 运行一次。2026-09-21 约 15:10 起间歇超时、16:05 起全断，当时公网 IP 为 126.166.241.49。
