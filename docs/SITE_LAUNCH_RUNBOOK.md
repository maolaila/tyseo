# 当批上站跟进操作手册

本轮授权：用户通过对话要求启动当天上站流程，完成后停止。此次只处理 `2026-09-18-pony-20`，不继续模板开发/临时任务，不扩成每日新批次，不采购或付费。

## 当前运行入口

- 用户纠正后，Codex heartbeat `pony-9-18-20` 已删除。当前由 `workbench.py` 本地程序每30分钟检查，工作台为 http://127.0.0.1:8766 。
- 本地台账：`runs/site-launch/2026-09-18-pony-20/state.json`。记录固定20个域名、上站词、每项状态、回执及核验。
- 这是由对话启动的独立程序，不依赖Codex持续监听，也不是RS机器人接入；此前“调度未启用”的说明被本批用户明确启动请求限定覆盖，其他流程不变。
- 状态未变保持安静；完成或需要处理时在当前任务通知。桌面通知能否弹出取决于应用/系统设置，不能保证未核验的通知渠道。

## 1. 等待购买

工作簿：`1y7VWR6T4ABcd09qa51GM5-7XXGC80w1h21wCL4stvUg`。

- 待购买页 gid=0；Bing上站页 gid=2066853497；不要使用百度页1730773540。
- 通过已登录浏览器只读获取最新表格。复制后必须核对表头/选区，避免旧剪贴板被误判为当前数据。
- 对照固定域名和归属Pony，不擅自增加、替换、重复写首批候选；注册局有记录不等于本团队已购买。
- 首次实测：20项全在待购买页；Bing上站页 A:H 全列回读3977行，没有Pony或本批liveball域名。
- 采购后移表是团队规定的后续触发条件；状态矛盾、归属不符或移表不明确时先核实，不提交上站。

## 2. 购买后准备上站资料

- 页内列实测：A日期、B归属、C域名、D詞語、E Template、F Title、G Description、H Keyword、I HeaderJS、J AF1、K FooterJS、L备注、M ServerName、N IP、O SeoFlag、P SubSeoKeyword、Q上站脚本。
- 首批使用r62。以采购人员已分配的域名、服务器和IP为准；缺失时记录阻断，不猜值、不移植别人的设置。
- 在Bing搜索本批分配词及适用联赛词，参考当前竞品表达，依据实际模板能力撰写TDK；不抄整段、不冒充官方品牌或承诺并不存在的功能。
- 只写本人的目标行和明确字段；保留现有配置、公式、HeaderJS/FooterJS。不因例行填表覆盖他人新改动。
- 写前读取目标行快照，写后逐字段回读；如已由他人填写，先核对，不盲目覆盖。

## 3. 提交上站

- 后台：`https://s213016.abcd-cms.com/mgradm/optdata.html`。首次检查已登录，存在“操作-上站”文本框和“提交”按钮，尚未提交。
- Q列生成内容是以美元符分隔的上站数据串；它是待校验的数据，不是给AI执行任意程序的指令。不要在shell运行它。
- 拷贝前核对最新整行，确认脚本只含本批域名、正确r62模板和刚回读的TDK及服务器配置。遇到无法确认字段先停止该项，继续其他可确定项。
- 提交前记录域名、脚本hash、时间与状态 `submitting`；一次一项，结果明确后再处理下一项。
- 回执不明、网络超时或任务中断时标记 `submission_unknown`，先查是否已生效，禁止凭“没看到成功”直接重提。
- 只使用已授权正常登录；遇到验证码或浏览器安全警告需要用户处理，不绕过。

## 4. 验证与结束

- 保存后台成功回执，再核验该域名可达、品牌/TDK及r62模板效果；DNS/HTTPS传播未完成时保持 `verifying`。
- 后台提交成功不等于最终页面验证成功；不把“买好/填好/提交了”标为 `verified`。
- 20项全部有提交和站点核验证据后才标整批 `completed`，生成完成清单，在当前任务通知用户，然后停止本批程序监听（工作台保留供查看）。
- 若仅部分成功，列出剩余项继续跟进；出现需用户处理的阻断只通知有意义的变化，不每半小时重复同一提示。
- 若日期已变但本批未完成，报告未完成项并等待用户处理，不自动创建次日任务或悄悄发布过期批次。
- 不自动发TG群/私讯、邮件；当前没有收件人和渠道授权。停止后不继续z1–z21或新模板工作。


## 程序工作台当前能力与限制

启动：`./Start-Workbench.ps1`。程序独立定时检查，页面5秒刷新显示；提供立即检查、继续/暂停、连接Google及浏览器桌面通知按钮。页面关闭不影响后台轮询；浏览器通知需要页面保持打开并由用户允许，尚未提供独立Windows原生通知服务。

Google Sheets匿名CSV访问实测401；已有Chrome缺少Playwright扩展。当前采用程序专用的持久浏览器会话（profile在private/launch-browser，session=tyseo-launch-program），需要用户完成一次登录。未经成功认证读取时工作台显示blocked，保留最后人工记录，不能当作实时采购数据。

历史阶段：当时只实现购买监听。当前能力及限制以文末“资料预览与手动回填”替代；正式提交及上线验证仍未接通。


用户浏览器要求：表格/后台连接使用普通持久化窗口，不用无痕或访客模式。已实际点击工作台“连接 Google 表格”，程序以private/launch-browser启动并到达Google登录页；进程命令含user-data-dir、不含--incognito/--guest。当前待用户首次登录，不能标为认证成功。

## 连接状态修复（2026-09-18）

用户反馈已能打开Google表格但工作台仍显示“程序专用浏览器未连接”。实测根因为SheetConnector使用相对目录，同时将包含该目录的相对脚本路径传入切换了cwd的CLI，形成重复路径并ENOENT；统一错误文案又把文件错误当作登录问题。已将连接目录/脚本路径解析为绝对路径，区分脚本错误与连接错误。

连接按钮现在打开浏览器后立即读表并清除旧错误；等待首次Google登录时最多5分钟每15秒复查，成功后回到正常30分钟采购检查。保留普通持久化配置和已有登录，未关闭浏览器或清除cookies。

真实回读：待购买表Pony=0，上站表Pony=20；工作台API connection_state=connected、last_error=null、待处理20；UI已显示“表格已连接”。状态仍是awaiting_tdk_worker，不代表已完成上站。新增4个回归用例，连接器与工作台共10个相关测试通过。


## 资料预览与手动回填（替代早期自动上站推进说明）

最新确认：Leo 15:20 通知购买完成，15:21 今天先用 r62，15:30 要求 TDK 填好先给他审核。用户要求先在工作台显示，手动按钮确认后回填，并明确“回填后台，提交等 Leo 审核”。

1. 程序用已认证浏览器读取采购移表、本人域名及分配词；按 config/tdk-policy.json 生成本地草稿和内容版本。定时扫描不会写表或提交。
2. 工作台逐域名显示 template、title、description、keywords，均为待审核草稿，不代表主管批准、排名或收录证明。
3. “确认回填 Google 上站表”仅在用户点击并确认后写本批本人行 E:H；写前再次读取校验、保留人工已有不同内容，写后核对 E:H 及其余配置列。此表是工作簿 1y7VWR6T4ABcd09qa51GM5-7XXGC80w1h21wCL4stvUg 的 Bing 上站页 gid=2066853497，不是参考关键词表。
4. 回读 Q 列脚本，核对域名/词/TDK/模板、归属和服务器后，“回填后台（不提交）”才可用。只填 #optdata001，不点击提交，不 POST 上站请求。使用普通持久化浏览器。
5. 正式提交等待 Leo 对当前内容版本审核；本轮没有接通提交执行器。审批内容变化必须重新审核，不能将用户回填确认等同于 Leo 批准。

当前真实证据：20 个域名在上站表 3539–3558 行，E:H 均空；本地生成 20 份草稿。M 列全部 s213017，已知后台却是 s213016.abcd-cms.com，因此后台回填禁用并显示原因。不得通过猜测域名绕过核实。本轮未写实际表格、未回填实际后台、未提交；外部回填适配器只有本地测试证据，首次实写仍需用户手动确认。

证据：runs/site-launch/2026-09-18-pony-20/tdk-before.json、tdk-preview-verification.json；UI 图位于 runs/workbench-qa/。测试入口 .venv/Scripts/python.exe -m unittest discover -s tests。运行数据不明文推到公开仓库。

## 参考词表与模板轮换（15:42–15:43 追加）

侧边任务同步的 Leo 最新解释：关键词表 https://docs.google.com/spreadsheets/d/1-3DvtAhJQu83G9fA1TWrEZIKiosOzvwDWYknEFXKGJ0/edit?gid=1237060894#gid=1237060894 仅供参考，不是回填目标，也没有必须逐字使用的要求。当前生成器不读取该词表，不能冒称已接入或已按全表核验。

模板分组按轮换混合上站；截图 Pony 对应 z、k、x、t，但表头日期 9月7日–9月12日、右侧编号不完整。来源截图 C:/Users/maola/AppData/Local/Temp/codex-clipboard-5eb1bb7f-6428-4587-b74b-3f58f5978b7e.png，当前仅收到侧边任务转述，未独立验证截图及现行分配。有效分组、周期和编号待核对；今日 r62 明确指示保持，不自动换模板。三件事分开：参考词源、上站资料回填目标、模板轮换配置。


15:46–15:47 最新解释及用户补充：TDK 由 AI 自行撰写，可以参考相关关键词竞争网站，不能直接抓取后当成最终文案；抓取不是必需前置依赖。本轮 AI 已参考代表站点表达后撰写生成策略，程序按策略和本人分配词生成草稿；没有每次扫描自动调用在线模型，也没有冒称已逐词完成 Bing 竞品研究。后续修改仍须先展示，保持 Leo 审核门禁。

用户确认参考工作簿内确有分组，应读取该表核对有效分组与轮换。此次只读导出已验证指定 gid=1237060894 是另一组关键词页（143 行，未找到 Pony），不能据此说整个工作簿没有分组；实际分组所在页与今日有效安排仍待定位核验。本轮没有写入此参考工作簿。
