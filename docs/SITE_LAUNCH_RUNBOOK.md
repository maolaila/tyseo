# Pony TDK 手动工作台

## 2026-09-21 上站模板默认值

用户转发 Leo 2026-09-21 11:02–11:06 回复链，明确纠正“默认 r62”：上站表 E 列 `Template` 必须从当时 `bing 指数词汇总` 中**分配给 Pony 的可用模板编号**里任选一项。2026-09-21 在线 `bing体育品牌词` 页 Pony 行 M17 实查 `z1~z17`；L17 的 `z、d、t、k` 是不同分组，不是可用编号清单。该范围由团队更新，每批在正式填 E:H 前重新只读核对归属、页签、位置和值，并选择其中一个实际可上站的编号；若缺失、歧义、已变更或选中模板未确认可用，暂缓模板/TDK 写入并核实。选版后按目标模板真实页面能力复核或重写本批 TDK。Pony 自己开发推送的 z18 不自动进入当前可上站范围；只有分配表日后把它纳入可用编号，才能选用。09-21 待采购批次的 20 份本地 TDK 草稿已撤销先前的 r62 标注，待正式填表时按最新可用范围选版。

**实现状态**：现有 `config/tdk-policy.json` 与 `src/tdk.py:generate_tdk` 仍按旧 r62 能力策略编写，工作台未实现读取最新模板分组。因此目前不能用旧固定选版程序声称已自动按此规则填表；人工/AI执行须按上段重新核对并留证。未来接入机器人时先适配、测试动态分组与缺失/变化阻断，不能仅改文字后自动运行。

## 2026-09-21 上站判据与机器人接入标准

1. 当批域名在“域名待购买”填写并回读保存后，便可开始**准备**当批原创 TDK：按有效分配词核对来源，研究当前竞品，确认目标模板真实能力，撰写并与本批、历史逐字段查重。此时只形成草稿，不因待购买表的删行/清空判断已购或写上站表。
2. 采购完成以对应日期的域名进入 Bing“上站202609”（gid=2066853497）为工作流判据。逐域名确认 A=对应日期、B=`Pony`、C=已登记域名、D=对应分配词，且唯一匹配；上站表没出现、日期/归属/域名/词不符或重复时保留待核查。待购买页是否删行不参与购买判定；即使仍保留同域名，只要上站表身份匹配便可推进 TDK。仅当两表的域名/分配词等实际身份信息矛盾时停下核查。
3. 上站表确认出现自己的行后，可按当前授权自动写该行 E:H（Template、Title、Description、Keyword）：写前核对空白或完全一致的旧值及本次文案版本；已有不同内容停止。写后回读 E:H，核对 A:D 和 I:P 不变并确认云端保存。机器人**接入后**在工作平台通知用户“本批 TDK 已填，可手动复制上站”，附批次日期、成功数量及实际行号；失败或部分完成报告确切行号和原因，不能发“全部完成”通知。当前机器人未接入，仍由对话报告，用户手动复制上站。
4. **仅在用户以后启用机器人自动上站时**：打开上站后台前逐行读取 M 列 `ServerName`，核对拟打开后台 URL 的服务名。用户提供 `https://s213017.abcd-cms.com/mgradm/optdata.html` 作为当前候选后台地址，不能固定成所有行的默认值。M 值为 `s213017` 时才与该 URL 主机相符；不同、空白、格式不明或批次混有不同服务时，停止受影响行后台操作并报告，不用历史 `s213016` 或截图值替代。图 1 显示的是 Curry 的 M 列示例，不是 Pony 当前行的证明。机器人接入、后台登录、脚本复制和正式提交均不在本次落盘任务中。

现有程序若仍把“待购买页含相同域名”当作拒写条件，属于旧实现；未来接入机器人前需按第 2 条调整并验证，不得仅凭本页文档声称程序已经自动执行这套新判据。

## 2026-09-19 用户最新补充

- 域名可用性核验是必要步骤：逐域名查注册状态，并在 Gname 等注册商核对可购买状态和价格，记录来源、时间和结果。RDAP 404 仅表示注册局无记录，不能直接写“已确认可购买”。遇验证码、登录或查询失败，标记“注册商待核验”，不得计为完成。今日已登记 20 个域名保持“RDAP 初查无记录、注册商待核验”，本条不虚构补验结果或授权购买。

- 用户说“上站”但没有提供新分配词时，默认使用前一天的有效分配词；提供新词则使用最新一批。在线表格用于核对参考，不因轮换区未出现 Pony 而自行换词或暂停已明确词库。域名后缀仅限 `.com`、`.cn`、`.com.cn`。
- 每日域名、TDK 都必须重新准备且不得重复。域名对当前批次、实时待购买页、上站历史页和本地历史记录查重；逐域名保存新的注册状态查询证据。
- 每批三项 TDK 根据真实页面能力及本次竞品研究重新撰写；各字段对本批和历史查重，不能仅改空格、标点或域名。持续使用同一词库不等于沿用旧 TDK。
- 此补充覆盖下文“在线分配没有 Pony 就待分配”的条件，现有明确词库继续有效；其他写表和人工后续边界保持不变。

2026-09-19 用户重新列明的当前 27 个词：球帝直播、说球帝直播、山猫直播、飞速直播、劲球直播、乐球直播、雨燕直播、看球吧、黑白直播、比赛直播、看个球、玩球直播、360直播、斗球直播、人人直播、抓饭直播、纬来体育直播、833直播、快直播、857直播、jrs直播、极直播、河豚直播、jrs直播吧、极速直播、蜘蛛直播、九球直播。

现行范围只到“把当次 TDK 填入对应上站表行并回读确认”。旧的采购监听、后台回填和提交说明保留于 SESSION_NOTES.md 作为历史，均不是当前工作台功能。后续上站步骤由用户手动处理。

## 每次上站前

**文案风格参考（2026-09-21 用户补充）**：按本次体育词查看 `bing 指数词汇总` 的相关页签。实查“排名数据All”有竞品标题等字段；“bing体育品牌词”“体育联赛词”主要是词、指数、分组。逐页签辨认实际字段：有标题/描述时借鉴词语搭配及表达节奏；无 TDK 字段时只作选词参考，不能补造 TDK。之后只读抽查上站表中 Leo 过往日期且与本批主题接近的 E:H，记录日期、行号、所参考的标题结构、描述长度和关键词组织。Leo 历史示例有“核心词＋赛事主题＋品牌”的标题结构，可借鉴层次和团队语气；不能直接套句、仅替换品牌或沿用未经证实的播放/画质/官方承诺。生成的新 Title/Description/Keywords 仍按本批词、真实站点内容和当次竞品自行撰写，并对本批/历史逐字段查重。指数表和 Leo 行全程只读，且 Leo 的文案风格参考不构成必须经 Leo 审核的新门槛。

1. 读取最新 bing 指数词汇总 工作簿（1-3DvtAhJQu83G9fA1TWrEZIKiosOzvwDWYknEFXKGJ0），核对当次日期、bing体育品牌词 页中的关键词分组及 Pony 实际归属。关键词分组与模板分组不可混淆。分配没有 Pony、日期已过期或表格含糊时记录“待分配”；不能拿旧截图的 z/k/x/t 或默认 all 代替。
2. 读取本次分配词、现有网站真实页面能力，并检查相关竞争网站的最新页面或 Bing 查询结果。排名数据All 可帮助找候选竞品，但旧抓取不等于当天已核实。保存参考网址与核对日期。AI 自行撰写 Title、Description、Keywords；不直接抓取竞品 TDK 充当原创文案，也不编造直播质量、官方身份或其他站点能力。
3. 以每次上站批次为单位建立新稿。同日再次上站也要重写；比较本批和历史批次的三项文案，逐字段拒绝完全相同或仅标点/空格不同的内容。历史指纹存于 data/tdk-fingerprints.json，并随工具仓库交付。Leo 审核只在当次明确要求时生效；2026-09-18 这批属于要求审核的特例。

新稿由当前对话中的 AI 按上述来源撰写、校验并放入本批本地台账；工作台不会无依据地生成固定句式。2026-09-18 本批 20 份已填，不因新规则被重写。当前截图的 9月14日–19日分配区未见 Pony；用户刚入职，不能推断当天归属，下一批须重新核实。

## 工作台唯一写入动作

启动 ./Start-Workbench.ps1，打开 http://127.0.0.1:8766/。工作台不在后台轮询，不自行定时创建任务；由用户对话触发当批工作。Google 表格使用普通持久化浏览器，不用无痕/访客。

点击“确认写入本批 TDK”时，程序只处理已确认的 Pony 域名：核对批次日期、域名/归属/分配词、当次文案版本及查重结果；写入 Bing 上站表工作簿 1y7VWR6T4ABcd09qa51GM5-7XXGC80w1h21wCL4stvUg 的 gid=2066853497。仅写 E:H（Template、Title、Description、Keywords），按域名逐行匹配；已有不同人工内容则停止。写后回读并核对 A:D 与 I:P 未被修改。参考关键词工作簿始终只读。

工作台完成 E:H 回读后即停止本次动作，不读取/复制 Q 列脚本、不打开后台、不提交网站、不验证上线。当前批次台账在 runs/site-launch/2026-09-18-pony-20/state.json；新批次通过启动参数 -State 指定独立台账，不自动续用旧日期。写入回执位于批次目录，历史文案只以指纹形式跟踪并在工具仓库提交。

## 已观察状态

- 2026-09-21：用户确认 09-19 批次 20 个域名已购买，在线 Bing 表匹配行 3610–3629。已重新研究、撰写并查重 TDK，按上一批 r62 填 E3610:H3629；A:P 320 单元格回读与预期一致，A:D/I:P 未变、云端保存已确认。本批指纹已入库；证据 `runs/site-launch/2026-09-19-pony/tdk-2026-09-21/`。后续由用户手动复制上站，未操作 Q 或后台。旧“注册商待核验”是原始查询记录，购买完成来自本次用户确认，不补造 AI 注册商核验结果。

- 2026-09-18 本批 Pony 20 行（3539–3558）已填入并通过 Google Sheets UI 回读：60 个 TDK 字段及 20 个 r62 模板字段对应正确，A:D/I:P 未变。Leo 通过来自用户转述；不能把它当作每批固定审批条件。
- 上述只证明 E:H 已填。Q 脚本、后台提交、网站上线和 Bing 抓取均不由当前工作台宣称完成。
- Playwright CLI 版本为 0.1.20。若 Google 返回 HTTP 429，按限流处理而非误判为登出；写入结果不明时先只读回查，不盲目重复粘贴。
