# 体育 Bing SEO 整站模板自动化工作流需求总纲

**项目：** `cms-sport-tpl-bing`  
**使用者：** Pony  
**版本：** v2.0 · 2026-09-18 · 实际项目适配版  
**交付对象：** 在用户本地工作区执行任务的 Codex  
**文档性质：** 开发需求、工程约束和验收契约；不是已经实现的生成器，不代表已完成真实项目测试。

> 产品目标：在现有 Python/Jinja 体育站项目中，持续制作视觉与布局明显不同、功能齐全、数据绑定正确、便于 Bing 发现和读取的整站模板；通过本地工作流完成审计、设计、生成、逐页测试、修复和交付。不得改动后端或其他旧模板，不得越界扩展已授权的域名工作流。

---

## 0. 阅读顺序、事实依据与优先级

Codex 首先读取本文件、`AGENTS.md`、`CODEX_START.md`、`docs/ACCEPTANCE.md`，再读取本地项目。仓库内已有的有效项目指令也要读取，不得用本包覆盖它们。

需求优先级：**用户最新明确指令 > 本次提供的团队约束 > 本地经核验的接口/模板契约 > 本文工程默认值 > 早期通用方案。** 遇到相互冲突的强约束，记录冲突并停止冲突部分；仍继续可独立完成的本地工作。

本文件中的事实来源：

| 标识 | 来源及可支撑的内容 |
|---|---|
| U1 | 用户此前确认：每两天交付一套模板，一套约十几个页面。这里的数量是业务描述，不是自动固定为 15 个路由。 |
| U2 | 2026-09-18 Leo 对话：主要做 Bing SEO；一套模板对应整个站；每套样式/排版不能重复；今日先选 20 个域名，暂时使用 r62；今日先把上站处理好。 |
| U3 | 2026-09-18 Sit 对话：Python 依赖、`python run.py`、本地 5000 端口；r62 为模板编号；HTML 是 Jinja；修改范围仅为两个模板目录；后端由 Rechard 负责。 |
| U4 | 2026-09-18 Sit 对话：RWD、dark/light、search、goto top、手机抽屉或合适的展开导航、AI SEO 检查、每页视觉验收；r62 可作为接口参考，但其前端实现可能不统一。 |
| U5 | 用户本次提供的 Codex U03 工作流及执行记录：域名只到“选域名→自动写待购买表”；20 个候选已写 A191:C210；尚未购买、上线或设置定时任务。 |
| R1–R13 | 文末官方技术资料。只有搜索引擎/工具机制由这些资料支撑；本文的矩阵、阈值、阶段和架构是本项目的工程设计。 |

**本次资料核验范围：** 已读取用户完整粘贴的聊天和现有两步工作流；GitHub 连接器读取指定仓库返回 404，未取得源码；公开 r62 示例站未取得可用页面内容。因此本文没有宣称已确认项目的框架名称、文件清单、变量名、搜索接口或运行效果。Codex 的第一阶段必须在用户已跑通的本地项目中补齐这些事实。不将 404 推断为仓库不存在，也不要求用户重新搭建已完成的环境。

### 0.1 本版替代早期方案的哪些假设

- 不再另建 Nunjucks/静态站框架来替代业务项目；保留现有 Python/Jinja 服务端渲染体系。
- 不再把“一套模板”解释为十几篇内容不同的新闻详情页；交付的是现有站点页面体系的一套皮肤。
- 不以 Google 专用接口和规则作为 Bing 项目的执行依据。
- 不以随机换色、类名或 DOM 作为模板差异化的主要方式。
- 不把域名选择、TDK、采购、部署、IndexNow 提交和反馈统计拼成已获准自动执行的完整链路。
- 不把工具脚本、测试依赖或根目录 `AGENTS.md` 加进公司仓库，突破两个模板目录的改动边界。

## 1. 已确认业务模型

### 1.1 分清四个对象

| 对象 | 本工作流中的含义 |
|---|---|
| 模板版本 | 某个 template_id 下的一套 Jinja、CSS、JS 和静态资源。 |
| 页面类型 | 首页、列表、详情等实际存在的路由与模板组合；以本地审计为准。 |
| 网站实例 | 一个域名及其站点配置、内容和选用模板；多个站点可暂时复用同一模板。 |
| 内容 URL | 网站中的某篇资讯、某场赛事、某页列表等实际页面地址。 |

**20 个站不等于今天开发 20 套新模板。** U2 已明确今日复用 r62。模板开发节奏与每日上站节奏分开建模，不能将两个数量相乘来生成任务。

“一套十几个页面”也不等于“十几个 Jinja 文件”：包含文件、宏和基础布局并不是独立页面；动态详情路由可能对应大量内容 URL。验收覆盖页面类型和状态，不靠凑文件数。

### 1.2 本次目标与非目标

本次必须交付可本地运行、可重复执行的**模板生产工作流**。首个业务模板是在工作流跑通后产生的验证产物，而不是用一个漂亮首页冒充完成整套系统。

不做：重写 Python、数据库/接口改造、重新配置 IP 白名单、替换现有 CMS、新闻采集系统、直播源获取、自动买域名、自动上线、定时调度、跨站链接网络、伪造抓取流量、自动发群消息、自动合并 PR。后续确有新增需求，再以独立任务纳入。

## 2. 两条工作流隔离与现有授权保持

### 2.1 已实现的域名流程：只保留，不重做

既有本地记录路径：`C:/tyseo/cms-sport-tpl-bing/.local-records/domains/WORKFLOW.md`。

当前规则原样保持：

1. 按**当次明确数量**从分配词库选域名，仅允许 `.com`、`.cn`、`.com.cn`，记录注册状态查询的来源、时间、限制；20 仅为首批数量。
2. 自动写指定工作簿的“域名待购买”页（gid=0），A=归属 `Pony`，B=域名，C=上站词。先查重、找真实空白区域，不覆盖他人/备注/词库；写入后逐行回读并确认云端保存。已获授权，不增加逐条审批。

工作簿标识：`1y7VWR6T4ABcd09qa51GM5-7XXGC80w1h21wCL4stvUg`。聊天中另一个 gid 不能替代用户后来明确的 gid=0。

用户提供的首批记录：2026-09-18，A191:C210，共 20 行，归属 Pony，已回读；只排除了待购买页重复，未声称检查所有历史页。Verisign RDAP 无记录；Gname 有验证限制，注册商可购性和价格未确认。**这些是用户提供的执行记录，本次未重新访问表格核验。**

不得重新写这 20 行；不得把 RDAP 无记录提升为“可购买/已购买”；不得根据这些信息自动发布。

### 2.2 本次新增模板流程：独立入口

新增模板任务只接收 template_id、参考模板、设计要求和已核验页面契约。它不调用表格写入，不触发域名采购，也不自动读取域名表后为每行生成一套模板。

可预留只读的 `site_id → domain → template_id → version` 关联接口，但尚无正式实例映射时保持空值，不能从 A191:C210 擅自推断已部署站点。

### 2.3 TDK 的边界必须精确

- **本次要做：** 保留和正确渲染现有 title、description、keywords 数据绑定；检查缺失、错误继承、硬编码、转义及页面主题一致性。
- **本次不做：** 根据新域名/上站词自动批量撰写正式站点 TDK、更新上站配置、写入其他工作表。
- keywords 是团队现有字段，按契约保留；不宣称堆满关键词能增加抓取。
- 无正式站点配置时，只能输出本地测试数据和待接入事项，不能把测试文案作为生产内容交付。

团队“中午前”“一点左右”的时区未确认。本次没有定时任务，不推算北京时间或日本时间并创建调度。

## 3. 系统架构与文件写入边界

### 3.1 两个工作区

建议目录结构如下；外部路径可以配置，不要求公司更换目录：

```text
C:/tyseo/
├── cms-sport-tpl-bing/                # 已有业务仓库
│   ├── templates/r62/                # 参考：只读
│   ├── static/r62/                   # 参考：只读
│   ├── templates/{new_template_id}/  # 本任务允许的业务写入范围
│   ├── static/{new_template_id}/     # 本任务允许的业务写入范围
│   ├── .local-records/domains/       # 已有域名流程，不修改
│   └── 其他已有文件                   # 本任务只读，不修改
└── pony-template-workflow/           # 新建的外部本地工具工作区
    ├── AGENTS.md
    ├── src/                         # 工作流、规则引擎、浏览器测试
    ├── tests/
    ├── config/
    ├── tasks/
    ├── catalog/                     # 模板设计档案与核验信息
    ├── fixtures/                    # 仅测试数据，不发布
    └── runs/{run_id}/               # 报告、原始响应、截图、修复记录
```

外部工具建议 Node.js/TypeScript + Playwright；这是**工具侧推荐**，不是新增业务前端框架。若用户已有适合的工具实现，则复用。工具运行时不应成为生产网站依赖，也不在业务仓库添加 package.json、锁文件、CI、全局样式或测试工程。依赖具体版本在实施时锁定并记录。

Jinja 编译/校验必须使用项目实际版本和可用环境；不得用 Nunjucks 近似执行代替。需要调用 Python 时，从外部工具调用现有解释器或外部只读校验脚本；不得修改或新增业务仓库中的 Python 文件。

### 3.2 允许与禁止的变更

业务允许列表精确展开为本次一个目标编号的两个目录，而不是允许写 `templates/**`、`static/**`。

禁止修改：任何 `.py`、requirements、`.env`、路由、数据库、接口、项目根配置、共享父模板、其他 template_id、域名记录、GitHub Actions。不要为了给 AI 加规则而覆盖公司根目录 `AGENTS.md`。本包 `AGENTS.md` 属于外部工具目录；从业务目录启动 Codex 时通过任务提示明确读取它，并读取业务已有指令。[R10]

允许只读理解 Python、路由和配置结构以提取契约；不得把读取权限理解为修改权限。报告只记录需要的配置键名，不保存 `.env` 内容、令牌、数据库连接串或白名单 IP。

### 3.3 变更边界必须用程序检查

开始时记录 base commit、当前分支、已暂存/未暂存/未跟踪文件和已有用户改动；每次写文件前校验实际解析路径，阻止 `..`、符号链接/Windows junction 逃逸和大小写路径混淆。

结束时比较工作区增量与允许列表，并检查受保护文件指纹。已存在的无关改动不应被当成本任务成果，也不能清理。禁止 `git reset --hard`、批量 `git clean`、覆盖用户工作树或自动重写提交历史。

**路径门禁不是对任意工具写入的绝对沙箱保证。** 写入前检查和交付前审计都要实现；出现越界变更立即阻断交付，定位来源，保留证据，不擅自回滚混有用户改动的文件。

运行缓存/日志应使用现有或外部指定的本地缓存路径，单独记录可接受的运行副作用；不得通过笼统允许缓存来放行源码变更。

## 4. 阶段 A：只读项目审计与页面契约

用户环境已经运行，首步是**确认现状并提取契约**，不是重新安装、改 .env 或申请白名单。

### 4.1 审计范围

读取项目已有说明、r62 的 Jinja/CSS/JS、被引用的公共文件、路由和渲染逻辑；按需读取其他近期模板作兼容与设计参考。仅从已列出的真实文件继续追踪，不猜文件名或接口。

形成以下产物：

| 文件 | 必须记录 |
|---|---|
| `project-audit.md` | 真实框架/版本/启动方式、当前模板选择机制、资源寻址、缓存行为、只读范围、未决事项。 |
| `page-contract.json` | 每个页面类型的路由模式、真实样例 URL、Jinja 入口、include/extends/macros、变量、过滤器、数据状态、导航/搜索/分页契约。 |
| `reference-baseline/` | r62 各页面原始响应、截图、现有错误与有效行为，不把所有旧问题当成新模板回归。 |
| `dependency-map.json` | 模板/资源引用图；共享只读依赖与可复制到新模板的实现区分。 |
| `backend-requests.md` | 缺字段、缺接口、路由/状态码/SEO 输出问题，按责任交给 Rechard；没有问题也明确写“本轮未发现”。 |

### 4.2 每条页面契约至少包括

```text
page_type_id、entry_template、route_pattern、sample_urls
required/optional_variables、data_source、observed_value_types
filters、macros、includes、extends、context_dependencies
navigation_links、pagination_contract、search_contract
seo_fields、canonical_policy、indexing_policy
required_content_selectors、key_function_selectors
data_states、baseline_issues、evidence_file、audit_status
```

不知道的值填 `unknown`/null 并列出所缺证据，不能补成看似合理的变量或接口。页面数量从“可达路由 + 模板引用图”得到，不把宏/局部组件计为路由。动态赛事至少选择真实可用样例，再用测试夹具补边界。

### 4.3 数据与页面边界

第一版只重排**当前页面已经能取得的数据**。首页没有某类数据，不能因为设计图需要就把详情数据搬到首页、拼私有接口或新增 Python。确需变化时生成后端请求，采用现有可行设计继续其他工作。

保留原有 `url_for` 或项目 URL helper、分页参数、搜索表单参数、JS data 属性及真实链接的语义。保留继承/宏 context 的行为，不凭变量名相近就替换字段。

### 4.4 新模板编号与本地预览

先查团队命名/分配方式和目标目录是否已存在。不得因 r62 存在就自动认定 r63 归 Pony。未取得有效编号时，可以完成工具、审计和外部草稿，不能写入一个擅自占用的业务编号。

预览优先使用项目现有模板选择入口、或经源码验证有效的**进程级**环境覆盖，并记录是否实际生效。不得猜测 .env 键名，不自动修改 .env。若现有程序不支持无文件改动的切换，输出一个准确的本地配置依赖；外部夹具仍可验收工具，但不得称为新模板实机已通过。

模板 ID、站点品牌、真实域名、上站词是不同字段，不能将目录名当品牌名或 canonical 域名。

## 5. 阶段 B：设计规格与整站差异化

### 5.1 可复用的是功能契约，不是每次同一张皮肤

必须同时满足：每套内部风格一致；不同套之间有明确的布局/风格区别；数据、路由、SEO 基本语义稳定。

可复用：搜索逻辑、主题切换逻辑、抽屉交互、比分字段规则、URL helper 的调用约定、测试规则。可将成熟逻辑复制为新模板内自包含实现。

不得新增依赖其他模板私有目录的引用，例如新模板引用 `static/r62/app.js` 或继承 `r62/base.html`；团队既有共享依赖只能按审计确认的方式继续使用，不修改它们。

### 5.2 设计蓝图产物

生成 `design-brief.md` 和 `design-spec.json`：

- 当前模板与参考模板编号、实际页面清单、目标气质、字体层级、配色 token、明暗主题、间距、圆角、图标策略。
- 首页主区域组织、导航模式、赛事行/卡片样式、列表密度、详情布局、侧栏处理、移动端结构。
- 每个页面类型的线框和模块顺序；只能使用页面已有数据。
- 与 r62 和近期可用模板相比，具体改了哪些结构；附对应页面对比索引。
- 搜索/比分/导航/分页的可见性和内容顺序说明。

没有设计稿时由工作流自行产出方案，不反复询问纯装饰性选择。涉及新增业务内容或接口才形成依赖。先完成首页、一个最具代表性的赛事/列表页、一个详情页的结构与样式，再扩展全部页面；这不是省略其余页验收的理由。

### 5.3 差异化验收

工程默认：在首页组织、导航、赛事展示、资讯列表、详情结构等主维度中，至少三项有可指认的布局差异，同时形成独立视觉语言。这是内部预检阈值，**不是 Leo 对“完全不能重复”的正式量化解释，也不保证主管验收通过**。

只换颜色、字体、阴影、图片、类名、随机 DOM 顺序、隐藏占位块均不足。不得为了拉低相似度而牺牲语义或手机可读性。

比较应尽量使用相同测试内容、相同视口和主题；去除比分实时变化造成的无关噪声。图像/结构相似度只能提示复核，不作为“Bing 认为原创”或“公司必定认可”的证明。

模板代码复用与内容重复是不同问题。换布局并不能消除多个域名上相同内容的重复性；Bing 对重复内容与 URL 管理有独立要求。[R2] 本工具不自动跨站 canonical、不搭站群互链、不实施爬虫伪装或隐藏关键词。

## 6. 阶段 C：Jinja、资源与页面生成

### 6.1 输出要求

业务成果只写目标的两个目录。模板保持 Jinja，而不是把某一次页面响应硬编码成静态 HTML。所有实际页面类型都必须有实现；允许页面继承目标目录内基础布局和宏。

先构建目标模板内的设计 token、header/nav、footer、搜索、主题、goto top 和关键体育组件，再实现页面。不要一次性把 r62 各文件做全局文本替换后直接交付。

### 6.2 数据绑定规则

不发明 JSON 字段、API、赛事链接、作者、直播状态或比分。测试假数据和真实站点数据必须标识和分开保存；假数据不得写进生产 Jinja/静态 JS。

区分变量未定义、null、空字符串、数值 0、字符串 `"0"`、false。比分 0 是有效数据，不是“缺失”。Jinja 的 `default(..., true)` 会处理假值，应避免用它给比分兜底；具体行为以项目实际 Jinja 版本和字段类型验证。[R6]

缺少可选数据可以显示明确空态；缺少必需字段要报出契约失败，不能使用 `|default('')` 大面积吞掉缺失字段，制造“页面无报错”的假象。

保留现有转义/可信富文本契约；不批量新增 `|safe`，也不擅自双重转义破坏正文。模板重构须保留宏作用域、include 上下文、自定义过滤器和合法资源引用。[R6][R7]

### 6.3 静态资源与性能

优先用项目已有可用资源和本地轻量实现；不为搜索/抽屉/主题引入完整 SPA 框架。不新增未经说明的远程字体、统计脚本或大型依赖。

CSS 使用 token 和组件规则；针对比分、队名、logo、时间和状态做稳定布局，不靠固定像素绝对定位拼整行。JS 初始化可重复调用且不重复注册监听，模块按需存在，任意页面缺某个 DOM 元素不应报错。

图片有明确占位尺寸和失败回退；非首屏图片可按实际情况延迟加载，不能让首屏关键内容或主要图片必须滚动才出现。减少不必要阻塞，但不将某个 Lighthouse 分数当作抓取保证。

## 7. 团队要求对应的功能规范

以下均来自 U4；详细实现与测试是本工作流提出的验收标准。

### 7.1 RWD：整站而非首页适配

覆盖每个实际页面类型。桌面、平板、手机均可访问主要内容和链接。允许数据表在自己的明确容器内横向滚动，不允许整张页面横向溢出、比分被裁切或主操作落到屏幕之外。

长标题、长队名、空数据、图片失败、三位数比分、筛选器换行、较大文字等都要检查。不要通过缩小至难读字号、隐藏比分/队名或 `body {overflow-x:hidden}` 掩盖布局错误。

### 7.2 dark/light theme

全页面共用目标模板内的主题 token；背景、正文、弱化文字、比分、链接、输入框、边框、菜单、焦点、错误态、空态、图标均覆盖。

首次无选择时可跟随系统，用户手动选择优先；跨页/刷新保持；本地存储不可用时仍能切换而不崩溃。无 JavaScript 时有完整可读的 CSS 回退，不依赖主题脚本显示正文。

普通正文默认按 WCAG 2.2 AA 的 4.5:1、大字 3:1 对比度要求检查，适用例外按标准处理；自动检查不能覆盖全部视觉场景，仍需逐页复核。[R12]

### 7.3 搜索 search

优先复用审计确认的站内搜索路由、表单参数、结果模板和分页行为。不凭空写 `/search?q=`；不把“搜索按钮存在”当成功能完成。

必须测试：输入、Enter、按钮提交、中文、空格与特殊字符、空输入、无结果、结果详情跳转、结果分页（若存在）、返回后状态、移动端可操作性。结果标题和入口在返回 HTML 中可读取。

如果已有搜索仅筛选当前页，必须标注其实际范围；不能伪称全站搜索。如果完全没有搜索能力，记录依赖并给 Rechard 明确请求，不擅自改后端。可以实现不冒充全站搜索的本页过滤，但不能自动把它记作团队 search 要求已满足。

站内搜索结果的索引策略从现有项目提取，不能为了“可抓取”一律移除 noindex，或生成无限搜索词 URL。

### 7.4 goto top

每个适用内容页提供返回顶部入口；长页滚动后可见、可点击、可键盘操作，不遮挡比分、分页或手机底部操作。尊重减少动画偏好；短页可以不显示无意义按钮，但须有明确适用性说明。优先提供正常页内锚点作为无 JS 回退。

### 7.5 手机导航

使用汉堡入口打开 drawer/popover 或合适的展开导航，符合 U4，不采用桌面导航挤成一排的临时方案。

链接本身仍为原始 HTML 中的真实 `<a href>`。有明确打开/关闭状态、可见关闭按钮、当前栏目标识。模态 drawer 实现焦点进入/约束/关闭后返回、Esc、背景不可交互；非模态展开面板采用对应语义，不能仅加 `aria-modal=true` 冒充模态。[R11]

测试关闭后页面可滚动、连续开关、导航后状态、手机旋转/切桌面断点、菜单超长可滚动、软键盘与搜索共存。禁用 JS 时仍保留可访问的主要导航入口，可用默认展开或等价非脚本导航。

### 7.6 不破坏既有能力

原模板实际存在的分页、栏目切换、赛事日期筛选、直播入口、相关内容、面包屑、视频/图集等均在审计后纳入回归。这里不代表确认 r62 拥有所有列举能力，也不授权新建不存在的业务功能。

如保留既有实时刷新逻辑，不擅自改变请求频率或数据源；更新时不应导致比分/队名位置明显跳动。真实直播源、播放器可用性由既有系统负责；本工具不采集或伪造源地址。

## 8. 体育专用：比分与赛事展示的阻断级验收

聊天已出现“比分跑掉”的反馈。比分既要**绑定正确**，也要**布局稳定**；截图好看但主客队对调同样是失败。

### 8.1 必测数据状态

| 测试状态 | 预期 |
|---|---|
| 未开始、比分为 null/未定义 | 按既有业务语义显示 VS/待赛/占位，不能编造 0:0。 |
| 正在比赛 0:0 | 两个 0 都正常显示，不变成空白、横线或 VS。 |
| 0:1、1:0 | 零分一侧不能消失，不互换主客队。 |
| 足球两位数/篮球三位数 | 12:10、118:115 等不挤出列、不截断。 |
| 长中文/英文队名 | 队名可合理换行/截断并保持完整可访问名称，比分不被压缩或覆盖。 |
| 缺 logo、logo 加载失败 | 有稳定占位；布局不坍塌，不出现无限 onerror 重试。 |
| 延期/取消/中场/完场 | 按真实状态映射显示；不把所有状态统一写成直播中。 |
| 半场、加时、点球等附加比分 | 仅当真实契约提供时显示，并与主比分区分；不能自行拼凑。 |
| 时区/日期跨日 | 使用后端/项目确认的时区规则，不因用户在日本而把全部赛程改成日本时间。 |
| 空列表和异常数据 | 给出真实空态/错误态；不填假赛事，不吞掉必需字段错误。 |

### 8.2 测试分层

数据断言检查“真实字段值→可见文本”，布局断言检查关键区域包围盒、溢出和遮挡，截图检查整体协调。元素相交未必总是错误，因此碰撞判断使用组件级预期和白名单，不写一个全页面任意重叠都报错的粗糙规则。

对比分关键区域不能使用截图 mask 或把元素隐藏来消除差异。动态状态用外部固定夹具稳定；真实运行样本再检查初始/更新后的显示与绑定。任何新引入的比分缺失、错误、裁切、主客队错位为阻断交付问题。

## 9. Bing 抓取友好的模板规范

目标是让页面可发现、可获取、可理解，并保留正确索引信号；不是承诺“写成某种 HTML 就会高频抓取”。Bing 官方强调真实内链、可读取内容、URL 一致性和减少无价值重复。[R1]

### 9.1 直接由模板控制的规则

| 编号 | 要求 | 检查证据 |
|---|---|---|
| SEO-01 | 页面主题、主要赛事/资讯、详情入口及当前分页信息在初始服务端 HTML 中。 | 保存 HTTP 响应体，解析实际业务文本和链接；不只看浏览器最终 DOM。 |
| SEO-02 | 主要跳转使用真实 `<a href>`；按钮用于操作，不用 onclick 代替详情/栏目发现入口。 | 原始响应的链接清单及可达性结果。 |
| SEO-03 | title、description、团队 keywords 字段保持真实绑定，不能全站硬编码首页 TDK。 | 多页面/多站点配置样本的 head 快照；模板变量来源。 |
| SEO-04 | 标题层级和主要内容结构清楚，语言属性正确。一个主 H1 是本项目默认，不宣称多个 H1 必然导致不收录。 | DOM/源 HTML 检查和人工阅读。 |
| SEO-05 | 保留后端 URL helper、业务路由和当前分页参数。内容不同的分页不应随意全部声明首页为规范页。 | page-contract 中的 canonical_policy 与实际 HTML 比较。 |
| SEO-06 | canonical 根据站点与页面契约输出，不残留 r62 演示域名、localhost 或上个站域名。无法由现有数据可靠得到时报告依赖。 | 已知站点配置下的预期值；不从未经核验的 Host 头自行构造。 |
| SEO-07 | 分页有独立可请求地址和真实链接；“加载更多”只能增强，不成为全部后续内容的唯一入口。 | 首/中/末页请求与链接链路；沿用真实 URL 形式。 |
| SEO-08 | 移动端保持主要内容与链接；JS 禁用时可浏览主体和主要导航。 | 无 JS 的手机/桌面测试。 |
| SEO-09 | 图片文本与实际内容相符；不把标题、比分、关键赛事信息仅做成图片或 CSS 伪元素。 | 文本/图片语义与视图检查。 |
| SEO-10 | robots meta 与 X-Robots-Tag 分开观测；对“预期可索引页”误加 noindex 必须失败。 | 原始响应头/HTML 与每页索引策略。 |
| SEO-11 | 不残留占位符、调试内容、测试品牌、错误主题、伪造作者/时间；JSON-LD 与可见内容一致。 | 源码/响应/结构化数据联合核验。 |
| SEO-12 | 不硬编码全站重复段落、不堆关键词、不插隐藏链接，不使用只对爬虫显示的内容。 | 规则检查与 AI 内容审查。 |

以上是工作流的项目规则。canonical 既不是强制收录命令，也不能替代真实重复内容治理；不得根据单一“self-canonical 规则”自动覆盖公司的跨域或重复页策略。疑似错误先记录证据并提交建议。[R2]

Bing 支持 HTML robots 指令和对应 X-Robots-Tag；`noindex` 与抓取限制含义不同。合法 noindex 的搜索结果/错误页不应因追求评分而被移除限制。[R3]

### 9.2 结构化数据采用保守、准确策略

先保留并验证现有正确实现。真实新闻详情可在字段充足时输出相符的 Article/NewsArticle；赛事页只在存在可验证字段和明确需求时考虑 SportsEvent 等；列表不是一篇新闻。模板不能虚构作者、评分、赛事状态、时间或组织名称以凑 schema。

没有适配某类结构化数据不自动判定抓取失败；错误或误导的数据要报错。JSON-LD 检查包括 JSON 语法、类型/字段、URL、时间和可见文本的一致性。

### 9.3 不能通过改模板单独解决的事项

robots.txt、Sitemap 输出、服务器状态码、缓存键、域名 canonical 配置、搜索路由、CDN/WAF、DNS/HTTPS、IndexNow 密钥/提交和站点内容策略可能由后端/运维/SEO 控制。本轮只做可达范围内的读取、问题记录和接入说明；**不改 Python，不自动修生产配置。**

Sitemap 可帮助 Bing 发现页面，但不能替代所有站内入口；现阶段只检查/记录现有策略，不自动生成并提交新站 Sitemap。[R4]

IndexNow 作为未来发布后通知接口保留说明，默认禁用；即便提交成功也不等于已抓取或收录。[R5]

### 9.4 AI SEO 检查的证据要求

AI 接收该套每个页面类型的渲染 HTML 摘要、head、链接图、结构化数据、截图和自动测试结果。输出规则 ID、证据位置、问题、影响、责任层和建议；不能只给一个“SEO 95 分”。

检查必须覆盖所有页面类型，可以分批，不得只看首页后推断全站。每条结论有 `pass / fail / blocked / needs_review / not_applicable`。无法读取线上数据时写 blocked 或未观测，不编造 Bing 测试结果。

## 10. 自动验收体系

### 10.1 验收顺序

```text
数据/路径契约
  → Jinja/资源检查
  → 原始响应/SEO检查
  → 功能测试
  → 全页面RWD与主题测试
  → 体育边界测试
  → AI逐页视觉与SEO复核
  → 按证据自动修复
  → 全量回归
  → 交付包与人工验收清单
```

### 10.2 默认测试矩阵

定义 N 为审计确认的、需要交付的独立页面类型数量。项目默认值不是搜索引擎标准：

| 测试层 | 覆盖范围 |
|---|---|
| 原始 HTTP/HTML | N 个页面全部，加分页和关键数据状态样例；检查内容、状态、head、链接和错误。 |
| 主浏览器布局/主题 | Chromium：每个页面 × 360、390、768、1280、1920 CSS px × light/dark。 |
| 无 JS | 每个页面 × 390 与 1280 CSS px；主体、主要导航/链接/分页可用。 |
| 交互 | 每个页面实际提供的搜索、抽屉、主题、goto top、分页/筛选；共享组件仍检查各页挂载。 |
| 体育边界 | 所有实际使用比分组件的页面；第 8 节夹具状态 × 手机/桌面 × 明暗。 |
| 兼容性补充 | WebKit/Firefox 对首页、赛事/列表、详情、搜索及手机导航做关键链路；若运行环境不可用，报告 blocked，不假称通过。 |
| 极端宽度/文字 | 320 CSS px、200% 文字缩放等对关键页专项检查；问题回归到组件测试。 |

例如 N=13，则主布局/主题矩阵为 130 个用例，无 JS 为额外 26 个用例，其他状态另计。**只生成这些截图不算“逐页看过”**：需要自动断言、逐页 AI 检视记录和方便人工查看的索引。

Playwright 可模拟视口、颜色方案和禁用 JS。[R8] 浏览器、操作系统、字体和渲染环境影响截图比较；固定工具版本和基线环境，不承诺像素完全跨环境一致。[R9]

### 10.3 真实运行与外部夹具分开

A. 真实应用验收：使用用户已有 Python 程序提供的目标模板，读取真实样本，证明路由/渲染/数据接入。

B. 确定性测试：在外部测试目录准备脱敏/合成夹具，以现有 Jinja 环境的可用规则或已核验的响应机制测试 0 分/长队名等边界。夹具验证模板逻辑与布局，不证明线上数据接入。

夹具不得进入生产目录，不修改后端来添加测试接口，不掩盖真实网络失败。真实应用不可用时只能交付“工具/夹具通过、实际模板联调未验证”的报告。

截图稳定化应等待已知业务选择器、字体和图片状态；有实时轮询时不盲目等待 networkidle。可以禁用动画/固定夹具时间，但不能隐藏比分、导航或其他被验收元素。

### 10.4 报告、严重度与交付状态

- **Blocker/P0：** 越界改动、主体空白/Jinja 500、必需页面缺失、比分内容错误/核心区域错位、搜索主链不可用、严重移动溢出、生产测试数据泄漏、预期可索引页误 noindex 等。不得标记可交付。
- **Major/P1：** 明暗主题局部不可读、重要图片/内链错误、drawer 关闭后页面不可用、明显跨页不一致等。修复后全量回归。
- **Minor/P2：** 非关键间距、装饰细节等。列明剩余项，不自称零缺陷。
- 外部既有问题单独标注 baseline/dependency，不掩盖，但也不误称本模板新增回归。

状态至少分开：`workflow_implemented`、`fixture_verified`、`real_app_verified`、`automated_quality_passed`、`ai_review_completed`、`human_review_pending/accepted`、`deployed`、`bing_crawl_observed`。后两项本轮默认为未执行/未观测。

本地质量门禁通过可生成“待人工验收”交付包；不得写成 Leo/Sit 已验收、线上已发布或 Bing 已抓取。AI 的视觉判断也可能漏错，报告必须方便 Pony 快速浏览全页，并保留主管最终验收。

## 11. 自动修复与恢复执行

工作流不是“一次生成→人工找全部问题”。每轮自动检查后，把失败项整理为针对具体页面/组件的修复任务，Codex 在允许目录内修复并重新验证。

修复输入必须有规则 ID、复现条件、截图/HTML/日志、期望与实际、疑似责任组件。优先修复共享组件，再回归所有使用页面；不能仅给当前截图添加特判 CSS。

默认每阶段最多三轮自动修复，可配置。这是防止无限循环的工程默认值，不是时长承诺。连续无改善、需要后端、新增权限范围或素材缺失时，将相关部分标记 blocked，并继续独立可做项。

严禁为通过检查而删除断言、缩小必测范围、掩盖失败区域、将 fail 改 not_applicable、任意更新视觉基线或放宽质量阈值。新设计建立首个基线时，必须保留本轮原图和待复核状态，不把首次截图自动当成正确设计。

每轮记录 `run_id`、输入哈希、契约版本、文件增量、测试结果、修复摘要、剩余问题。支持中断续跑：校验上次输入与文件是否变化，受影响环节重跑；不能沿用已过期的 PASS。模板任务结束后不自动转入采购、部署或下一批域名任务。

## 12. 数据模型与对外接口契约

### 12.1 TaskSpec

至少包含：

```text
schema_version、task_id、mode、repo_root、workflow_root
reference_template_id、target_template_id、target_id_confirmed
page_contract_path、design_spec_path、preview_base_url
browser_matrix、repair_round_limit
remote_writes_enabled=false、deployment_enabled=false
site_tdk_generation_enabled=false、domain_workflow_mutation_enabled=false
indexnow_enabled=false、scheduler_enabled=false
```

`mode` 为 `bootstrap / new-template / repair-template / verify-template`。允许工具初建阶段 target_template_id=null；这只允许审计和外部工具/草稿工作，不允许写业务模板目录。

项目目录从用户实际工作区确定。默认示例中的 Windows 路径用于本地环境，不在其他机器上自动创建同名业务仓库。没有模板编号或页面契约时给出精确依赖，不以猜测代替。

### 12.2 PageContract

字段见 4.2，必须引用真实源码路径/行段或响应证据，并记录审计时间与 commit。Jinja 变量“必需/可选/状态相关”分别定义，避免所有变量一律必填或一律给空默认。

`canonical_policy` 与 `indexing_policy` 是数据化规则，不应从页面 URL 名字猜测。不同站点内容下的参数由已有配置提供；任务没有正式域名也能做本地逻辑测试，但生产 URL 信号标为未验证。

### 12.3 DesignSpec

记录布局结构和视觉 token、light/dark、跨页组件契约、对比模板、差异证据与未满足项。格式可以 JSON/YAML，但规则引擎必须有对应 Schema；不能只存一段自由描述后声称可重复生成。

### 12.4 Finding/TestResult

```text
rule_id、page_type_id、sample_url、viewport、theme、data_state
environment(real_app/fixture)、severity、status
expected、actual、evidence_paths、owner_layer
baseline_or_regression、fix_round、manual_review
```

没有执行证据的记录不能是 pass。not_applicable 必须说明适用条件与依据；blocked 不等于 pass。输出 JSON 供续跑，输出 Markdown/本地 HTML 索引供人阅读。

### 12.5 TemplateCatalog

记录 template_id、版本/commit/工作树哈希、创建人、设计摘要、页面类型、截图索引、最近验收结果、可用的实例映射。近期模板样式对比可利用此目录，不需要每次把整个仓库喂给模型。

不得将 unknown 状态变成“已上线/有效模板”；不能因最近目录存在就认定主管验收通过。

## 13. 每次交付内容与Git边界

业务代码：目标 `templates/{id}` 和 `static/{id}` 的新增/修改文件，保留现有集成方式。

外部证据包：

```text
runs/{run_id}/
├── task-resolved.json
├── project-audit.md
├── page-contract.json
├── design-brief.md
├── design-spec.json
├── source-manifest.json
├── changed-files.json
├── raw-responses/                    # 脱敏；头与正文分开
├── screenshots/                     # page/theme/viewport/state 可定位
├── report.json
├── report.md
├── review-index.html                # 全页缩略图、失败项、原图链接
├── ai-seo-review.md
├── ai-visual-review.md
├── design-differences.md
├── backend-requests.md
├── repair-history.md
└── handoff.md                        # 给 Pony/Sit/Leo 的本轮说明草稿
```

报告不是正式公司记录；不在本轮自动发群、填上站表或提交到公司 PR。用户后续明确要求提交/创建 PR 时，按既有分支约定执行，并只包含业务允许目录；不自动合并。没有远程写授权时，输出可审查补丁/文件清单和 PR 描述草稿，不推送。

变更报告必须列出：完整页面清单、与 r62 的主要区别、全部必需功能覆盖、比分状态测试、SEO 结果、实际环境验证范围、未决后端问题、剩余风险、尚未部署/尚未观测 Bing 的状态。

## 14. 后续扩展接口：只记录，不启用

### 14.1 上站/TDK/部署

未来用户给出真实流程后，可将域名记录与模板版本建立映射，再独立实现 TDK 审核、站点配置、发布前检查、发布结果回读、失败回滚与通知。当前不实现自动副作用，不把“聊天曾提到每天上站”理解为这些动作已被本次授权。

### 14.2 SEO反馈

预留只读导入接口，不伪造结果。未来记录 domain、URL、template_id、版本、生效时间、内容版本、验证的爬虫身份、响应类型和首次成功抓取时间；新域名和旧域名、不同内容/更新时间分组比较，避免把域名因素都归因给模板。

Bingbot 身份不能只凭 User-Agent。可以使用官方验证方式，例如反向 DNS 后正向核对，或官方公布且定期更新的 IP 列表；验证不了归为 unknown。[R13]

可以约定 D7 去重成功抓取覆盖率等内部观察指标，但 D7 不是 Bing 抓取期限承诺。HTML 抓取与图片/CSS/JS请求、自然抓取与自有检测、200 与错误状态、正常 304 验证要区分。索引、曝光、点击分别记录，不能用抓取次数代替全部效果。

本模块现阶段仅定义数据接口/说明，不要求连接 Bing Webmaster 或线上日志才能完成本地模板工作流；没有线上访问权不虚构在线验证。

## 15. Codex实施计划与每阶段退出条件

以下阶段是实施顺序，不是未来交付时间估计；每阶段都要留可验证产物。

| 阶段 | 实施内容 | 退出条件 |
|---|---|---|
| M0 | 阅读指令、保护域名记录、只读审计 r62、提取页面/变量/路由/搜索契约。 | 输出真实项目审计与完整页面清单；未知项有依据；业务仓库零意外改动。 |
| M1 | 外部本地工具骨架、TaskSpec校验、路径/增量门禁、run记录、可恢复执行。 | 能拒绝无效任务、越界写入和编号冲突；工具自己的测试通过。 |
| M2 | 设计规格、模板档案、目标编号校验、代表页生成与本地预览适配。 | 目标数据绑定不变；差异不只是换色；预览真实状态准确记录。 |
| M3 | 完成所有页面与六项功能、体育组件边界、自包含资源。 | 页面覆盖完整，必需功能和比分测试通过，无额外后端修改。 |
| M4 | 自动HTML/SEO、Playwright矩阵、AI逐页复核、修复循环、报告索引。 | 全套证据完整；必测失败已修复或明确阻断，不使用虚假PASS。 |
| M5 | 新增第二个不同设计的验证任务，证明工作流可复用；打包说明与维护手册。 | 非从零手工重复；可配置换任务，输出不同布局；两套都有独立报告。 |

M5 的第二套先在外部草稿/夹具证明可复用；只有团队确认第二个编号后才写入业务仓库。**验证工作流不授权占用两个正式编号，也不要求入职当天交付两套。**

先完成有用的 CLI/脚本和报告，不先开发完整后台、任务队列、数据库或云端 Agent 平台。由当前 Codex 会话统筹设计、生成与修复，稳定的检查和打包用普通程序。若现有套餐/工具不支持视觉审阅，保留人工复核和 blocked/needs_review，不默默新购 API；不把月度200美元描述为无限调用额度。

## 16. 日常使用流程与验收成功标准

工作流实施后，Pony 的一次新模板任务应是：

```text
给出/读取有效模板编号与本批设计方向
  → 工作流审计差异并锁定页面契约
  → 生成设计与代表页
  → 自动扩展完整页面
  → 自动逐页检查、看图和修复
  → Pony查看证据索引并确认交付
```

默认连续执行不涉及新授权的本地步骤，不反复问“是否继续下一页”。缺业务字段/模板编号/生产操作权限时只阻断相关部分，不把整个工具实现搁置。局部审美选择由设计规格解决，不增加不必要审批。

必须支持按同样流程修复现有指定模板；repair-template 模式必须明确目标和修改范围，不能因为测试旧模板发现错误就顺手修改所有旧模板。

最终验收由三层组成：

1. **工具层：** 门禁、任务校验、测试发现、报告和续跑机制确实可执行；失败夹具能被检出。
2. **模板层：** 全部页面、必需交互、比分、主题、RWD、HTML/SEO规则和差异化证据齐全；实际程序联调单独有结果。
3. **交付层：** 业务增量仅在允许目录；无秘密/夹具混入；域名旧流程未变；未擅自推送/上线/提交爬虫；结果可人工复核。

不能以“生成了十几个HTML”“Lighthouse分高”“首页截图好看”“AI说SEO通过”代替上述验收。

## 17. 未决信息的处理清单

| 信息 | 如何处理，不重复问用户已知事项 |
|---|---|
| 实际页面数/文件名/变量/路由 | Codex 从本地源码和运行页面发现并记录。 |
| r62 搜索实现与参数 | 只读审计+真实交互核验；缺能力则产生后端请求。 |
| 模板编号分配 | 优先读取团队既有规则；没有则外部草稿，不能猜 r63。 |
| 模板切换与缓存机制 | 核验项目配置/读取顺序，不自动改 .env/后端。 |
| 设计差异的正式尺度 | 本文先给内部门槛与对比证据；主管实际反馈再固化，不承诺自动判定公司验收。 |
| 页面合并或新增数据 | 不做；形成 Rechard 需求记录，其余模板工作继续。 |
| 正式TDK/采购/部署步骤 | 保持未启用；不根据历史群聊自行补完。 |
| 团队截止时区/永久每日站数 | 仍未明确；不设调度、不将20固定成日常配置。 |
| 真实Bing绩效口径/日志权限 | 作为后续只读反馈接口；不阻断当前本地工作流。 |

## 18. 官方技术参考与来源边界

核验日期：2026-09-18。以下引用用于校验技术机制，不证明公司具体实现。工具版本仍以实际安装/锁定版本为准。代码块中的网址是参考来源，不是本次已调用的生产接口。

| 引用 | 官方资料 | 适用内容 |
|---|---|---|
| R1 | Bing Webmaster Guidelines | 内链、可读取内容、清晰HTML、抓取效率与不当SEO方式。 |
| R2 | Bing：Does Duplicate Content Hurt SEO and AI Search Visibility?（2025-12） | 重复内容与URL治理，换皮不等于内容差异。 |
| R3 | Bing：Robots meta tags and attributes that Bing supports | robots与X-Robots-Tag、noindex语义。 |
| R4 | Bing：Sitemaps | Sitemap发现与站长工具。 |
| R5 | IndexNow FAQ | 提交通知不保证抓取/索引。 |
| R6 | Jinja Template Designer Documentation | 默认值、变量、宏、include、继承、tojson等真实语义。 |
| R7 | Jinja API | Undefined/StrictUndefined及转义环境等，不授权修改业务环境。 |
| R8 | Playwright Emulation | 视口、颜色方案、禁用JS的测试能力。 |
| R9 | Playwright Visual comparisons | 截图对比及环境差异。 |
| R10 | OpenAI：Custom instructions with AGENTS.md | 指令文件发现/作用域；外部文件不自动在所有目录生效。 |
| R11 | W3C WAI APG：Dialog (Modal) Pattern | 模态导航的焦点/关闭/背景交互语义。 |
| R12 | W3C WCAG 2.2：Contrast (Minimum) | 正文及大字对比度标准与例外。 |
| R13 | Bing：How to Verify Bingbot | 验证真实爬虫，不单看User-Agent。 |

```text
R1 https://www.bing.com/webmasters/help/bing-webmaster-guidelines-30fba23a
R2 https://blogs.bing.com/webmaster/December-2025/Does-Duplicate-Content-Hurt-SEO-and-AI-Search-Visibility
R3 https://www.bing.com/webmasters/help/robots-meta-tags-and-attributes-that-bing-supports-5198d240
R4 https://www4.bing.com/webmasters/help/sitemaps-3b5cf6ed
R5 https://www.indexnow.org/faq
R6 https://jinja.palletsprojects.com/en/stable/templates/
R7 https://jinja.palletsprojects.com/en/stable/api/
R8 https://playwright.dev/docs/emulation
R9 https://playwright.dev/docs/test-snapshots
R10 https://developers.openai.com/codex/guides/agents-md/
R11 https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/
R12 https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html
R13 https://www.bing.com/webmasters/help/how-to-verify-bingbot-3905dc26
```

**本包当前完成的仅是需求、契约示例和实施指令。真实项目审计、生成器实现、页面渲染、截图、功能测试与Bing观察均由后续本地实施产生，不得把本文件的要求描述成已经执行的测试结果。**
