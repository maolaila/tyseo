# 给Codex的实施入口

## 1. 推荐放置位置

将本包解压到独立目录，例如 `C:/tyseo/pony-template-workflow/`。公司业务仓库仍为 `C:/tyseo/cms-sport-tpl-bing/`；不把整个包复制进业务仓库。

在Codex能访问这两个本地目录的工作区中执行。若当前会话只开放一个目录，通过其正常的工作区配置开放外部目录；不要为了绕过限制将工具写进公司源码。

以下任务是本次需要Codex实施的需求；本包没有已经实现的CLI命令，不要直接运行不存在的 `workflow generate` 等命令。

## 2. 首次实施提示词（直接粘贴）

```text
你现在需要为我的体育Bing SEO工作实施一个本地自动化模板生产工作流。

先阅读这个外部工作流目录中的PRD.md、AGENTS.md、docs/ACCEPTANCE.md、
config/task.example.json及config/rules.json，再读取业务仓库已有指令。
业务仓库在C:/tyseo/cms-sport-tpl-bing，已完成环境配置，当前r62能正常显示赛事。
实际路径若有差异，以当前工作区已有项目为准，不另建替代业务项目。

本次不是生成十几篇新闻页，而是为现有Python/Jinja整站生成新模板。
一套约十几个页面，每两天交付一套；实际页面类型/文件/变量/搜索路由须先只读审计。

第一阶段完成本地审计、页面/数据/路由/资源契约、r62基线。
随后在外部工作流目录实施任务配置、路径门禁、设计规格、生成、逐页浏览器测试、
原始HTML与Bing SEO检查、自动修复、报告索引和交付流程。
工具脚本、依赖、夹具、截图、报告不放进公司仓库。

公司业务仓库本次只允许修改已确认目标编号的templates/{id}和static/{id}。
不改Python、requirements、.env、路由、共享文件或其他模板；不得覆盖r62。
不要自动编号r63。缺有效目标编号时，完成工具与外部草稿，不占用正式目录。
不要为了预览改后端；先查现有模板选择机制，使用已核验有效的本地方式。

每套必须包含完整页面体系、RWD、dark/light、可实际工作的搜索、goto top、
手机drawer/popover导航，并在所有页面执行视觉和SEO检查。
重点检查比分0值、未开始空值、三位数比分、长队名及手机明暗模式；
不得把假数据、硬编码域名、失效链接或假搜索带进交付。
不同模板的主体布局与视觉要显著不同，不允许只换色/类名。

保留已有域名工作流和.local-records/domains/WORKFLOW.md，不重做，不改写首批20行。
本次不自动处理TDK业务文案、采购、发布、IndexNow、定时任务、表格写入或消息发送。
也不自动push、创建PR、合并。

按PRD的M0至M5推进。边界内的本地设计、生成、测试与修复连续执行，
不要每个页面都问我是否继续；缺接口/编号等真实依赖时报告并继续独立可做部分。
先做能用的工作流，不先搭复杂后台/数据库/队列。

每阶段给出：实际完成项、改动文件、可复现测试证据、未完成/被阻断项。
最终把工具已实现、夹具已通过、真实项目已通过、人工待验收、未部署、
未观测Bing抓取这些状态分开，禁止虚构完成或抓取效果。
```

## 3. 后续新模板任务提示词

```text
读取现有PRD、项目契约和模板档案，开始一套新模板任务。
目标模板编号使用本次已由团队确认的编号；没有确认编号则只做外部草稿。
参考r62的数据与功能契约，结合最近模板对比，提出主体布局不同的新设计，
再生成全部实际页面。设计不得要求新后端字段，也不搬用其他页面才有的数据。

完成全部页面的RWD、明暗主题、搜索、返回顶部、手机导航、比分边界、
原始HTML/SEO、AI逐页视觉复核、自动修复及全量回归。
更新本地模板档案和交付报告；不触发域名、TDK、采购、上线或远程Git操作。
```

## 4. 反馈修复提示词

```text
读取本轮主管反馈和对应模板报告，定位到指定template_id、页面、状态、视口与主题。
只修改本任务明确的两个模板目录；不要修改后端、其他模板或旧域名记录。
先增加能够复现该问题的测试，再修复组件；全量回归该组件出现的所有页面。
输出修复前后截图、数据断言、变更范围和剩余问题，不自动推送或上线。
```

## 5. 用户需要看什么

优先看生成后的 `review-index.html`、`report.md` 和 `handoff.md`；索引必须包含每种页面类型的手机/桌面、light/dark视图及失败项。缺截图或缺真实联调证据的页面不能被摘要隐藏。
